#ifndef RB_PATH_TRACING_RAB_LIGHT_SAMPLING_HLSLI
#define RB_PATH_TRACING_RAB_LIGHT_SAMPLING_HLSLI

// Compatibility wrapper for old bridge includes. New pass-local shaders should
// include only the light capabilities they need.
#include "RAB_LightSamplingCore.hlsli"
#include "RAB_LightTarget.hlsli"
#include "RAB_Visibility.hlsli"
#include "RAB_DuplicationMap.hlsli"

#endif
