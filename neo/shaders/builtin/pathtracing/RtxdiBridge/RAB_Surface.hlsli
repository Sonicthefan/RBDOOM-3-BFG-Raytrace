#ifndef RB_PATH_TRACING_RAB_SURFACE_HLSLI
#define RB_PATH_TRACING_RAB_SURFACE_HLSLI

// Compatibility wrapper for old bridge includes. New pass-local shaders should
// prefer RAB_SurfaceCore.hlsli when they do not need BRDF sampling/evaluation.
#include "RAB_Brdf.hlsli"

#endif
