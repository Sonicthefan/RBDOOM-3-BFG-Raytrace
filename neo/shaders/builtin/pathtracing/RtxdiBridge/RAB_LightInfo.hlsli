#ifndef RB_PATH_TRACING_RAB_LIGHT_INFO_HLSLI
#define RB_PATH_TRACING_RAB_LIGHT_INFO_HLSLI

// Compatibility wrapper for the current smoke/ReSTIR bridge. New pass-local
// shaders can include RAB_LightInfoCore.hlsli when they only need light records.
#include "RAB_LightInfoRuntime.hlsli"

#endif
