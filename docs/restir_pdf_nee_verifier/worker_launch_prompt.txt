Paste-Ready Worker Launch Prompt
================================

Use this prompt for a new agent.


Prompt
------

Repository:

    E:/prog/rbdoom-3-bfg-rt

Branch:

    codex/restir-rab-reference-audit-experiment

Task:

    Work on the replacement ReSTIR PDF + NEE current-frame producer only after
    auditing the working RLU -> clean RTXDI DI path. Do not edit temporal
    accumulation code unless explicitly instructed in a later task.

Read first:

    docs/restir_pdf_nee_verifier/README.txt
    docs/restir_pdf_nee_verifier/contract_matrix.txt
    docs/restir_pdf_nee_verifier/cvar_contract.txt
    docs/restir_pdf_nee_verifier/io_whitelist.txt
    docs/restir_pdf_nee_verifier/validation_matrix.txt
    docs/restir_pdf_nee_verifier/worker_protocol.txt
    docs/restir_pdf_nee_verifier/worker_tasks.txt
    docs/restir_pdf_nee_verifier/rlu_04_current_status_and_parity_notes.txt

Known state:

    Clean RTXDI DI view 12 can consume the Remix Light Universe (RLU) dense
    light domain and visibly accumulate real Doom lighting. Treat that as the
    known-good producer/consumer baseline. Do not modify it.

    The current risk is the replacement current-frame estimator: source PDF,
    solidAnglePdf, targetPdf, selected-light replay, visibility, and final
    NEE/direct-light contribution when selecting from the RLU dense current
    domain.

    SourcePolicy 0 is the full current RLU dense uniform baseline. SourcePolicy
    1 is now the RLU-04 range-stratified current policy:

        sourcePdf = rangeSampleCount/(rangeCount*totalProposalSamples)
        invSourcePdf = (rangeCount*totalProposalSamples)/rangeSampleCount

    Ignore RLU payload sourcePdf/sourceWeight for proposal PDF.

    Current blocker: PDFNEE/RLU Doom analytic lighting must match clean RTXDI
    DI view 12 for the same current RLU analytic domain. If lights are absent,
    shaped differently, or have different brightness in PDFNEE, fix current
    analytic load/replay parity before touching temporal accumulation.

    First ambiguity to eliminate: domain alignment. With only
    r_pathTracingRestirPdfNeeVerifierEnable 1, scene build selects mixed RLU
    domain 2 when r_pathTracingRemixLightUniverseEnable is off. If
    r_pathTracingRemixLightUniverseEnable is on, the explicit
    r_pathTracingRemixLightUniverseDomain value remains authoritative. Do not
    compare PDFNEE domain 0 against clean view-12 domain 2. Report the RLU
    domain and typed ranges for both paths before making a visual claim.

    Historical stale code text: older
    r_pathTracingRestirPdfNeeVerifierSourcePolicy CVar help may say policy 1 is
    typed 50/50. Treat that wording as stale wherever it appears; RLU-04 policy
    1 is range-stratified.

    The old PDFNEE/ReGIR verifier routes are discarded as implementation
    routes. Do not claim success from PDFNEE lightMode 8, PDFNEE lightMode 9,
    ReGIR debug views, portal candidate pools, mode 56, view 72, or
    non-crashing builds.

Default functionality rule:

    One enable CVar must activate the normal producer path with useful
    defaults. Extra CVars are diagnostics only. Do not build a module whose
    normal function requires a long CVar recipe.

Standing restrictions:

    no temporal proof for this task
    no spatial
    no best lights
    no denoiser/confidence/gradient path
    no fallback albedo or beauty output
    no mode-56 route
    no old rbdoom NEE records as proof
    no ReGIR as the default source
    no Doom portal candidate pool as the default source
    no split-domain fallback as the default source
    no broad light suppression as a fix
    no invented ReSTIR math
    no local duplicate RAB helpers unless exact equivalence is proven first

Required bridge:

    Use or exactly match:

        RAB_LoadActiveRrxLightInfo
        RAB_SampleActiveRrxPolymorphicLight
        RAB_GetLightSampleTargetPdfForSurface
        RAB_GetReflectedBsdfRadianceForSurface

First action:

    If starting from scratch, start with PDFNEE-RLU-00 only as a read-only
    RLU/clean-DI contract audit. No code changes.

    If continuing the current active issue, start with PDFNEE-RLU-05:
    compare PDFNEE/RLU current Doom analytic lighting against clean RTXDI DI
    view 12 for the same dense current RLU analytic domain before editing
    temporal or accumulator code.

    Report:

        exact RLU dense current producer domain
        exact shader active RRX RAB consumer path
        exact clean RTXDI DI current reservoir page contract
        exact selected light identity expected by the clean consumer
        where source PDF is produced for the full current RLU dense proposal
            domain
        confirmation that typed-range, bounded per-range, payload-weighted,
            portal, and ReGIR policies are deferred
        where solidAnglePdf is produced
        where targetPdf is produced
        where final contribution is produced
        first missing or ambiguous contract

Build lane if later implementation is approved:

    From E:/prog/rbdoom-3-bfg-rt/neo:

        cmake --build --preset win64-pt-dev-release

    Copy:

        E:/prog/rbdoom-3-bfg-rt/build-win64-pt-dev/Release/RBDoom3BFG.exe
        to
        E:/prog/rbdoom-3-BFG-prebuilt/RBDoom3BFG.exe

    Copy contents of:

        E:/prog/rbdoom-3-bfg-rt/base/renderprogs2

    to:

        E:/prog/rbdoom-3-BFG-prebuilt/base/renderprogs2

    Do not nest renderprogs2 inside renderprogs2.
