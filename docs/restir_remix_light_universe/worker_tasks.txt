Remix-Shaped Light Universe Worker Tasks
=======================================

Run tasks in order. Do not skip ahead.


RLU-00: Read-Only Remix Contract Audit
--------------------------------------

Goal:

    Audit the current rbdoom light-universe/remap producers against RTX Remix
    and RTXDI temporal light-index translation.

Reference file-equivalent:

    E:/prog/references/dxvk-remix-git/src/dxvk/rtx_render/rtx_light_manager.cpp
    E:/prog/references/dxvk-remix-git/src/dxvk/rtx_render/rtx_context.cpp
    E:/prog/references/dxvk-remix-git/src/dxvk/shaders/rtx/pass/common_bindings.slangh
    E:/prog/references/dxvk-remix-git/src/dxvk/shaders/rtx/algorithm/rtxdi/RtxdiApplicationBridge.slangh
    E:/prog/references/RTXDI-main/Libraries/Rtxdi/Include/Rtxdi/DI/TemporalResampling.hlsli

Required rbdoom read set:

    neo/renderer/NVRHI/PathTraceDoomLights.cpp
    neo/renderer/NVRHI/PathTraceDoomLights.h
    neo/renderer/NVRHI/PathTraceUnifiedLight.cpp
    neo/renderer/NVRHI/PathTraceUnifiedLight.h
    neo/renderer/NVRHI/PathTraceRemixLightManager.cpp
    neo/renderer/NVRHI/PathTraceRemixLightManager.h
    neo/renderer/NVRHI/PathTraceSmokeSceneBuild.cpp
    neo/shaders/builtin/pathtracing/cleanroom_rtxdi/pathtrace_clean_rtxdi_di_sentinel.rt.hlsl

Allowed write set:

    docs/restir_remix_light_universe only

Expected artifact:

    docs/restir_remix_light_universe/rlu_07_remix_portal_debug_crosswalk.txt

Forbidden:

    no code changes
    no build

Acceptance:

    report exact current producer deviations
    report exact replacement write set
    report exact RAB consumer boundary
    report first missing contract


RLU-01: Dense Domain CPU Shell
------------------------------

Goal:

    Add a default-off CPU shell for dense current/previous light domains and
    two-way mapping, with no active shader consume.

Reference file-equivalent:

    E:/prog/references/dxvk-remix-git/src/dxvk/rtx_render/rtx_light_manager.cpp

Required rbdoom read set:

    neo/renderer/NVRHI/PathTraceRemixLightManager.cpp
    neo/renderer/NVRHI/PathTraceRemixLightManager.h
    neo/renderer/NVRHI/PathTraceUnifiedLight.cpp
    neo/renderer/NVRHI/PathTraceUnifiedLight.h
    neo/renderer/NVRHI/PathTraceSmokeSceneBuild.cpp
    neo/renderer/NVRHI/PathTraceCVars.cpp
    neo/renderer/NVRHI/PathTraceCVars.h

Allowed rbdoom write set:

    neo/renderer/NVRHI/PathTraceRemixLightManager.cpp
    neo/renderer/NVRHI/PathTraceRemixLightManager.h
    neo/renderer/NVRHI/PathTraceSmokeSceneBuild.cpp
    neo/renderer/NVRHI/PathTraceCVars.cpp
    neo/renderer/NVRHI/PathTraceCVars.h
    docs/restir_remix_light_universe

Required:

    CVars from cvar_contract.txt
    dense current payload vector
    dense previous payload vector
    current-to-previous map
    previous-to-current map
    invalid sentinel
    one-shot dump
    disabled state

Forbidden:

    no clean RTXDI consume yet
    no shader temporal changes
    no spatial/best-light changes

Acceptance:

    build/deploy succeeds
    dump proves dense counts, mappings, and reset behavior


RLU-02: Doom Analytic Remix Mapping
-----------------------------------

Goal:

    Populate the dense domain from real Doom analytic lights and map current to
    previous by stable light identity.

Reference file-equivalent:

    E:/prog/references/dxvk-remix-git/src/dxvk/rtx_render/rtx_light_manager.cpp
    E:/prog/references/dxvk-remix-git/src/dxvk/shaders/rtx/algorithm/rtxdi/RtxdiApplicationBridge.slangh

Required rbdoom read set:

    neo/renderer/NVRHI/PathTraceDoomLights.cpp
    neo/renderer/NVRHI/PathTraceDoomLights.h
    neo/renderer/NVRHI/PathTraceSmokeSceneBuild.cpp
    neo/renderer/NVRHI/PathTraceUnifiedLight.cpp
    neo/renderer/NVRHI/PathTraceUnifiedLight.h

Allowed rbdoom write set:

    neo/renderer/NVRHI/PathTraceRemixLightManager.cpp
    neo/renderer/NVRHI/PathTraceRemixLightManager.h
    neo/renderer/NVRHI/PathTraceUnifiedLight.cpp
    neo/renderer/NVRHI/PathTraceUnifiedLight.h
    neo/renderer/NVRHI/PathTraceSmokeSceneBuild.cpp
    docs/restir_remix_light_universe

Required:

    analytic-only dense current domain
    analytic-only dense previous domain
    two-way mapping by stable renderLight/entity identity
    animated payload is payload-only, not structural invalidation
    spawned/despawned effects are local current-only/previous-only events

Acceptance:

    user dump shows stable room lights remain mapped while animated/effect
    lights do not poison unrelated mappings


RLU-03: Clean RTXDI View-12 Opt-In Consumer
-------------------------------------------

Goal:

    Add an opt-in clean RTXDI DI route that consumes the Remix-shaped analytic
    light universe instead of the old Doom analytic remap producer.

Reference file-equivalent:

    E:/prog/references/RTXDI-main/Libraries/Rtxdi/Include/Rtxdi/DI/InitialSampling.hlsli
    E:/prog/references/RTXDI-main/Libraries/Rtxdi/Include/Rtxdi/DI/TemporalResampling.hlsli
    E:/prog/references/dxvk-remix-git/src/dxvk/shaders/rtx/algorithm/rtxdi/RtxdiApplicationBridge.slangh

Required rbdoom read set:

    neo/shaders/builtin/pathtracing/cleanroom_rtxdi/pathtrace_clean_rtxdi_di_sentinel.rt.hlsl
    neo/shaders/builtin/pathtracing/RtxdiBridge/RAB_LightLoadRuntime.hlsli
    neo/renderer/NVRHI/PathTraceSmokeDispatch.cpp
    neo/renderer/NVRHI/PathTraceSmokeResources.cpp
    neo/renderer/NVRHI/PathTraceSmokeSceneBuild.cpp

Allowed rbdoom write set:

    neo/renderer/NVRHI/PathTraceSmokeDispatch.cpp
    neo/renderer/NVRHI/PathTraceSmokeResources.cpp
    neo/renderer/NVRHI/PathTraceSmokeSceneBuild.cpp
    neo/shaders/builtin/pathtracing/cleanroom_rtxdi/pathtrace_clean_rtxdi_di_sentinel.rt.hlsl
    docs/restir_remix_light_universe

Required:

    opt-in CVar only
    old path is legacy/debug only and is not proof or replacement fallback
    bypass remains diagnostic
    RAB_TranslateLightIndex maps through dense Remix-shaped maps
    no RTXDI math changes

Acceptance:

    build/deploy succeeds
    stop for user visual test of view 12 temporal off/on


RLU-04: Animated Payload / Effect-Light Proof
---------------------------------------------

Goal:

    Add focused diagnostics proving payload animation and spawn/despawn events
    are local.

Reference file-equivalent:

    E:/prog/references/dxvk-remix-git/src/dxvk/rtx_render/rtx_light_manager.cpp

Allowed rbdoom write set:

    neo/renderer/NVRHI/PathTraceRemixLightManager.cpp
    neo/renderer/NVRHI/PathTraceRemixLightManager.h
    neo/renderer/NVRHI/PathTraceSmokeDispatch.cpp
    docs/restir_remix_light_universe

Required:

    payload-only signature
    structural signature
    mapping signature
    current-only/previous-only counters
    duplicate identity counters

Acceptance:

    user can correlate sparks/robot lights with local mapping events while
    stable room lights remain mapped


RLU-05: Emissive Domain Addition
--------------------------------

Goal:

    Add emissive lights to the Remix-shaped universe after analytic proof.

Reference file-equivalent:

    E:/prog/references/dxvk-remix-git/src/dxvk/rtx_render/rtx_light_manager.cpp
    E:/prog/references/dxvk-remix-git/src/dxvk/shaders/rtx/pass/common_bindings.slangh

Required rbdoom read set:

    neo/renderer/NVRHI/PathTraceSmokeSceneBuild.cpp
    neo/renderer/NVRHI/PathTraceLightUniverse.cpp
    neo/renderer/NVRHI/PathTraceUnifiedLight.cpp

Allowed rbdoom write set:

    neo/renderer/NVRHI/PathTraceRemixLightManager.cpp
    neo/renderer/NVRHI/PathTraceRemixLightManager.h
    neo/renderer/NVRHI/PathTraceUnifiedLight.cpp
    neo/renderer/NVRHI/PathTraceUnifiedLight.h
    docs/restir_remix_light_universe

Required:

    final merged emissive list only
    typed analytic/emissive ranges
    no change to analytic temporal proof math

Acceptance:

    emissive debug mapping works without regressing analytic proof


RLU-06: Unified Runtime RAB Bridge
----------------------------------

Goal:

    Route active RAB light loading and translation through the Remix-shaped
    dense current/previous/mapping domain.

Reference file-equivalent:

    E:/prog/references/dxvk-remix-git/src/dxvk/shaders/rtx/algorithm/rtxdi/RtxdiApplicationBridge.slangh

Allowed rbdoom write set:

    neo/shaders/builtin/pathtracing/RtxdiBridge/RAB_LightLoadRuntime.hlsli
    neo/shaders/builtin/pathtracing/RtxdiBridge/RAB_LightSamplingCore.hlsli
    neo/renderer/NVRHI/PathTraceSmokeDispatch.cpp
    docs/restir_remix_light_universe

Required:

    RAB_LoadLightInfo current/previous dense-domain paths
    RAB_TranslateLightIndex current-to-previous and previous-to-current paths
    typed ranges remain inspectable

Acceptance:

    shader consumers no longer require legacy split payload reloads for the
    active proof path


RLU-07: RTX Portal / Remix Comparison Notes
-------------------------------------------

Goal:

    Document which rbdoom debug views and dumps correspond to RTX Portal /
    RTX Remix light-manager and RTXDI debugging concepts.

Allowed write set:

    docs/restir_remix_light_universe only

Forbidden:

    no implementation
    no build

Acceptance:

    exact rbdoom CVar/debug view crosswalk
    exact Remix/RTXDI reference file-equivalents
    explicit gaps that cannot be compared yet


RLU-08: Active Buffer Ownership Cleanup
---------------------------------------

Goal:

    Make the active polymorphic RLU buffer contract unambiguous for all
    follow-up DI, PDFNEE, NEE-cache, and ReGIR work.

Reference file-equivalent:

    E:/prog/references/dxvk-remix-git/src/dxvk/shaders/rtx/pass/common_bindings.slangh
    E:/prog/references/dxvk-remix-git/src/dxvk/shaders/rtx/algorithm/rtxdi/RtxdiApplicationBridge.slangh

Required rbdoom read set:

    neo/renderer/NVRHI/PathTraceSmokeResources.cpp
    neo/renderer/NVRHI/PathTraceSmokeSceneBuild.cpp
    neo/renderer/NVRHI/PathTraceUnifiedLight.h
    neo/shaders/builtin/pathtracing/RtxdiBridge/RAB_LightLoadRuntime.hlsli
    neo/shaders/builtin/pathtracing/cleanroom_rtxdi/pathtrace_clean_rtxdi_di_sentinel.rt.hlsl
    neo/shaders/builtin/pathtracing/pathtrace_smoke.rt.hlsl

Allowed write set:

    docs/restir_remix_light_universe only

Expected artifact:

    docs/restir_remix_light_universe/rlu_08_active_buffer_ownership.txt

Required:

    identify t66 as the only active current polymorphic RLU payload buffer
    identify t67 as the only active previous polymorphic RLU payload buffer
    identify t59/t60 as legacy/debug unless explicitly owned by a later task
    identify RAB_LoadActiveRrxLightInfo and RAB_SampleActiveRrxPolymorphicLight
        as the required active replay boundary
    identify NEE cache and ReGIR as proposal experiments, not replacement light
        universes

Forbidden:

    no code changes
    no build
    no CVar changes

Acceptance:

    docs say future DI proposal work must consume dense RLU indices through
    t66/t67 and RAB, not through private light ingestion or split fallbacks


RLU-09: Payload Replay Hardening Audit
--------------------------------------

Goal:

    Prove that current and previous RLU payloads are shader-replayable for
    emissives and Doom analytics before any clean DI sampling changes.

Reference file-equivalent:

    E:/prog/references/dxvk-remix-git/src/dxvk/rtx_render/rtx_light_manager.cpp
    E:/prog/references/dxvk-remix-git/src/dxvk/shaders/rtx/algorithm/rtxdi/RtxdiApplicationBridge.slangh

Required rbdoom read set:

    neo/renderer/NVRHI/PathTraceRemixLightManager.cpp
    neo/renderer/NVRHI/PathTraceSmokeSceneBuild.cpp
    neo/renderer/NVRHI/PathTraceUnifiedLight.cpp
    neo/shaders/builtin/pathtracing/RtxdiBridge/RAB_LightLoadRuntime.hlsli
    neo/shaders/builtin/pathtracing/RtxdiBridge/RAB_LightSamplingCore.hlsli
    neo/shaders/builtin/pathtracing/cleanroom_rtxdi/pathtrace_clean_rtxdi_di_sentinel.rt.hlsl

Allowed rbdoom write set:

    neo/renderer/NVRHI/PathTraceRemixLightManager.cpp
    neo/renderer/NVRHI/PathTraceSmokeSceneBuild.cpp
    neo/shaders/builtin/pathtracing/cleanroom_rtxdi/pathtrace_clean_rtxdi_di_sentinel.rt.hlsl
    docs/restir_remix_light_universe

Expected artifact:

    docs/restir_remix_light_universe/rlu_09_payload_replay_hardening.txt

Required:

    audit current and previous payload fields before upload and after shader
        replay
    cover radius, influenceRadius, area, luminance, sourceWeight, flags,
        sourceIndex, identityA, identityB, previousIndex
    preserve animated payload changes as payload-only unless identity changes
    prove failures are local to the selected light

Forbidden:

    no broad light suppression
    no private analytic-light reload path
    no shrinking authored Doom analytic lights to hide failures
    no changing clean DI reservoir math

Acceptance:

    RLU dump and clean debug view agree that replayable analytics and emissives
    remain valid while moving near animated lights, and unrelated lights are not
    poisoned


RLU-10: Authoritative Range And Sample Metadata
-----------------------------------------------

Goal:

    Make typed RLU ranges and bounded sample counts the only source of truth for
    follow-up clean DI proposal sampling.

Reference file-equivalent:

    E:/prog/references/dxvk-remix-git/src/dxvk/rtx_render/rtx_light_manager.cpp
    E:/prog/references/dxvk-remix-git/src/dxvk/shaders/rtx/algorithm/rtxdi/RtxdiApplicationBridge.slangh

Required rbdoom read set:

    neo/renderer/NVRHI/PathTraceRemixLightManager.cpp
    neo/renderer/NVRHI/PathTraceSmokeDispatch.cpp
    neo/shaders/builtin/pathtracing/RtxdiBridge/RAB_LightLoadRuntime.hlsli
    neo/shaders/builtin/pathtracing/cleanroom_rtxdi/pathtrace_clean_rtxdi_di_sentinel.rt.hlsl

Allowed rbdoom write set:

    neo/renderer/NVRHI/PathTraceRemixLightManager.cpp
    neo/renderer/NVRHI/PathTraceSmokeDispatch.cpp
    neo/shaders/builtin/pathtracing/RtxdiBridge/RAB_LightLoadRuntime.hlsli
    docs/restir_remix_light_universe

Required:

    RestirLightManagerRangeInfo.xy is emissive dense offset/count
    RestirLightManagerRangeInfo.zw is Doom analytic dense offset/count
    RestirLightManagerSampleInfo.x is bounded emissive sample count
    RestirLightManagerSampleInfo.y is bounded Doom analytic sample count
    RestirLightManagerSampleInfo.z is total proposal count
    RestirLightManagerSampleInfo.w is non-empty typed range count
    zero-count ranges are valid and must not invent fallback light domains

Forbidden:

    no shader-side range scanning as normal function
    no NEE-cache/ReGIR range ownership
    no changing payload layout unless the audit proves a layout bug

Acceptance:

    dumps report the same typed ranges and sample counts that shader helpers
    expose, and consumers can compute proposal PDFs from those values alone


RLU-11: Clean DI RLU Range Provider
-----------------------------------

Goal:

    Add a clean RTXDI DI helper layer that exposes Remix-style active light
    ranges from RLU metadata without changing reservoir behavior.

Reference file-equivalent:

    E:/prog/references/dxvk-remix-git/src/dxvk/shaders/rtx/algorithm/rtxdi/RtxdiApplicationBridge.slangh
    E:/prog/references/RTXDI-main/Libraries/Rtxdi/Include/Rtxdi/DI/InitialSampling.hlsli

Required rbdoom read set:

    neo/shaders/builtin/pathtracing/RtxdiBridge/RAB_LightLoadRuntime.hlsli
    neo/shaders/builtin/pathtracing/RtxdiBridge/RAB_LightSamplingCore.hlsli
    neo/shaders/builtin/pathtracing/cleanroom_rtxdi/pathtrace_clean_rtxdi_di_sentinel.rt.hlsl

Allowed rbdoom write set:

    neo/shaders/builtin/pathtracing/RtxdiBridge/RAB_LightLoadRuntime.hlsli
    neo/shaders/builtin/pathtracing/cleanroom_rtxdi/pathtrace_clean_rtxdi_di_sentinel.rt.hlsl
    docs/restir_remix_light_universe

Required:

    helper for current RLU typed range by light class
    helper for bounded sample count by light class
    helper for active non-empty range count
    helper for proposal probability / source PDF inputs used by clean DI
    all selected identities remain dense current RLU indices

Forbidden:

    no NEE cache provider in the normal clean DI path
    no ReGIR cell provider in the normal clean DI path
    no old Doom analytic array sampling
    no split-domain fallback for normal function
    no final lighting or temporal changes in this slice

Acceptance:

    clean DI can query the active RLU ranges in shader code, but output remains
    unchanged until RLU-12 consumes the helpers


RLU-12: Clean DI Typed Initial Sampling
---------------------------------------

Goal:

    Improve clean RTXDI DI current-frame proposals by sampling typed RLU ranges
    instead of treating the active light universe as one undifferentiated
    uniform region.

Reference file-equivalent:

    E:/prog/references/dxvk-remix-git/src/dxvk/shaders/rtx/pass/rtxdi/rtxdi_initial_sampling.comp.slang
    E:/prog/references/dxvk-remix-git/src/dxvk/rtx_render/rtx_light_manager.cpp
    E:/prog/references/RTXDI-main/Libraries/Rtxdi/Include/Rtxdi/DI/InitialSampling.hlsli

Required rbdoom read set:

    neo/shaders/builtin/pathtracing/cleanroom_rtxdi/pathtrace_clean_rtxdi_di_sentinel.rt.hlsl
    neo/shaders/builtin/pathtracing/RtxdiBridge/RAB_LightLoadRuntime.hlsli
    neo/shaders/builtin/pathtracing/RtxdiBridge/RAB_LightSamplingCore.hlsli
    neo/renderer/NVRHI/PathTraceSmokeDispatch.cpp

Allowed rbdoom write set:

    neo/shaders/builtin/pathtracing/cleanroom_rtxdi/pathtrace_clean_rtxdi_di_sentinel.rt.hlsl
    neo/renderer/NVRHI/PathTraceSmokeDispatch.cpp
    docs/restir_remix_light_universe

Required:

    stream emissive candidates from the emissive dense range when non-empty
    stream analytic candidates from the Doom analytic dense range when non-empty
    use the bounded sample counts supplied by RLU metadata
    compute sourcePdf from the actual proposal policy
    keep targetPdf from RAB_GetLightSampleTargetPdfForSurface
    keep solidAnglePdf from RAB_SampleActiveRrxPolymorphicLight /
        RAB_SamplePolymorphicLight
    finalize through RTXDI reservoir helpers and store M = 1

Forbidden:

    no hand-rolled target PDF
    no visibility folded into targetPdf
    no saturate-clamped output as proof
    no NEE-cache/ReGIR as default proposal source
    no temporal accumulation edits

Acceptance:

    clean RTXDI view 12 keeps the same mean lighting as the RLU baseline while
    candidate count and typed range sample counts produce predictable noise
    changes


RLU-13: Clean DI Previous-Best Initial-Sampling Seed
----------------------------------------------------

Goal:

    Add Remix-style previous-best seeding to clean RTXDI DI initial sampling
    after typed RLU sampling is correct.

Reference file-equivalent:

    E:/prog/references/dxvk-remix-git/src/dxvk/shaders/rtx/pass/rtxdi/rtxdi_initial_sampling.comp.slang
    E:/prog/references/dxvk-remix-git/src/dxvk/shaders/rtx/pass/rtxdi/rtxdi_temporal_reuse.comp.slang
    E:/prog/references/dxvk-remix-git/src/dxvk/shaders/rtx/pass/rtxdi/rtxdi_compute_gradients.comp.slang
    E:/prog/references/RTXDI-main/Libraries/Rtxdi/Include/Rtxdi/DI/Reservoir.hlsli
    E:/prog/references/RTXDI-main/Libraries/Rtxdi/Include/Rtxdi/DI/InitialSampling.hlsli

Required rbdoom read set:

    neo/shaders/builtin/pathtracing/cleanroom_rtxdi/pathtrace_clean_rtxdi_di_sentinel.rt.hlsl
    neo/shaders/builtin/pathtracing/RtxdiBridge/RAB_LightLoadRuntime.hlsli
    neo/renderer/NVRHI/PathTraceSmokeDispatch.cpp
    docs/restir_remix_rebuild/rrx_cf_05_active_di_previous_best_initial_sampling.txt

Allowed rbdoom write set:

    neo/shaders/builtin/pathtracing/cleanroom_rtxdi/pathtrace_clean_rtxdi_di_sentinel.rt.hlsl
    neo/renderer/NVRHI/PathTraceSmokeDispatch.cpp
    docs/restir_remix_light_universe

Required:

    project current pixel to previous pixel using the existing motion contract
    load previous selected light from an explicitly owned previous-best source
    translate previous-domain light to current-domain light with
        RAB_TranslateLightIndex(previousLight, false)
    reject invalid previous best, failed translation, or unsupported portal
        state locally
    build the translated current light as a normal single-light candidate
    combine it with random typed candidates through RTXDI reservoir helpers
    set M = 1 for the produced current reservoir

Forbidden:

    no claiming full Remix best-light parity if the source is the previous
        temporal reservoir instead of a real best-light feedback buffer
    no temporal reuse rewrite in this slice
    no spatial, gradient, confidence, or denoiser changes
    no private previous-light reload path

Acceptance:

    temporal-on movement preserves accumulated lighting better than RLU-12 in
    overlapping-light areas, without changing the stationary mean or poisoning
    animated analytic lights


RLU-14: Emissive Triangle Replay Correction
-------------------------------------------

Goal:

    Correct RLU emissive consume so emissive surfaces replay as actual
    emissive triangle area lights, not collapsed center/point-light proxies.
    The prior point/center-style emissive conversion attempt is not viable for
    the normal RLU proof path.

Reference file-equivalent:

    E:/prog/references/RTXDI-main/Samples/FullSample/Shaders/PrepareLights.hlsl
    E:/prog/references/RTXDI-main/Samples/FullSample/Shaders/PolymorphicLight.hlsli
    E:/prog/references/RTXDI-main/Samples/FullSample/Shaders/LightingPasses/RtxdiApplicationBridge/RAB_LightInfo.hlsli
    E:/prog/references/dxvk-remix-git/src/dxvk/shaders/rtx/algorithm/rtxdi/RtxdiApplicationBridge.slangh
    E:/prog/references/dxvk-remix-git/src/dxvk/shaders/rtx/algorithm/nee_cache_light.slangh

Required rbdoom read set:

    docs/restir_remix_light_universe/rlu_14_emissive_triangle_replay_correction.txt
    neo/renderer/NVRHI/PathTraceUnifiedLight.cpp
    neo/renderer/NVRHI/PathTraceUnifiedLight.h
    neo/renderer/NVRHI/PathTraceSmokeSceneBuild.cpp
    neo/renderer/NVRHI/PathTraceSmokeDispatch.cpp
    neo/shaders/builtin/pathtracing/RtxdiBridge/RAB_LightInfoCore.hlsli
    neo/shaders/builtin/pathtracing/RtxdiBridge/RAB_LightLoadRuntime.hlsli
    neo/shaders/builtin/pathtracing/RtxdiBridge/RAB_LightSamplingCore.hlsli
    neo/shaders/builtin/pathtracing/cleanroom_rtxdi/pathtrace_clean_rtxdi_di_sentinel.rt.hlsl

Allowed rbdoom write set:

    neo/renderer/NVRHI/PathTraceUnifiedLight.cpp
    neo/renderer/NVRHI/PathTraceUnifiedLight.h
    neo/renderer/NVRHI/PathTraceRemixLightManager.cpp
    neo/renderer/NVRHI/PathTraceSmokeSceneBuild.cpp
    neo/renderer/NVRHI/PathTraceSmokeDispatch.cpp
    neo/shaders/builtin/pathtracing/RtxdiBridge/RAB_LightInfoCore.hlsli
    neo/shaders/builtin/pathtracing/RtxdiBridge/RAB_LightLoadRuntime.hlsli
    neo/shaders/builtin/pathtracing/RtxdiBridge/RAB_LightSamplingCore.hlsli
    neo/shaders/builtin/pathtracing/cleanroom_rtxdi/pathtrace_clean_rtxdi_di_sentinel.rt.hlsl
    docs/restir_remix_light_universe

Required:

    define RLU emissives as replayable emissive triangle lights
    preserve dense RLU identity and current/previous remap
    exclude non-replayable emissive placeholders from dense current/previous
        RLU domains before range construction
    load or carry triangle positions and triangle UVs for the selected
        emissive record
    sample the actual triangle using barycentric UVs
    sample emissive texture/material at the sampled triangle UV when available
    compute areaPdf as 1 / triangleArea
    compute solidAnglePdf as area-to-solid-angle pdf using sampled distance and
        light-facing term
    trace visibility to the sampled triangle point
    reject emissive records that cannot replay as a real triangle in the normal
        clean DI path
    add diagnostics for replayable triangle count, missing geometry, missing
        material, zero area, zero radiance/sourceWeight, and center-fallback
        attempts

Forbidden:

    no center-position point-light fallback for emissive surfaces
    no treating center+area as a fake surface when triangle geometry is missing
    no aggregating multiple triangles into one surface light in this slice
    no new private emissive light universe
    no weighted emissive proposal changes before replay correctness is proven
    no temporal accumulation, spatial, denoiser, gradient, or previous-best
        changes

Acceptance:

    clean RTXDI view 12 emissive lighting follows visible emitting surfaces
    rather than producing point-light hotspots at triangle or surface centers
    center-fallback diagnostic count is zero in the normal clean DI path
    invalid emissive records are rejected locally without invalidating analytic
        RLU sampling or current/previous maps
    zero/dummy emissive placeholders do not occupy dense RLU emissive ranges
    toggling emissive texture use changes textured emissive contribution
    predictably without changing emissive identity or remap


RLU-15: Clean DI Basic Spatial Reuse
------------------------------------

Goal:

    Add a basic, default-on clean DI spatial reuse pass for the active view-12
    RLU lane after emissive triangle replay is proven.  This is a bridge for
    comparison against RTX Remix / RTX Portal debugging, not the later enhanced
    ReSTIR PT spatial technique.

Required rbdoom read set:

    docs/restir_remix_light_universe/rlu_15_clean_di_basic_spatial_reuse.txt
    neo/renderer/NVRHI/PathTraceCVars.cpp
    neo/renderer/NVRHI/PathTraceCVars.h
    neo/renderer/NVRHI/PathTracePrimaryPass.h
    neo/renderer/NVRHI/PathTraceSmokeDispatch.cpp
    neo/renderer/NVRHI/PathTraceSmokeResources.cpp
    neo/shaders/CMakeLists.txt
    neo/shaders/builtin/pathtracing/cleanroom_rtxdi/pathtrace_clean_rtxdi_di_sentinel.rt.hlsl

Allowed rbdoom write set:

    neo/renderer/NVRHI/PathTraceCVars.cpp
    neo/renderer/NVRHI/PathTraceCVars.h
    neo/renderer/NVRHI/PathTraceCleanRtxdiDiGui.h
    neo/renderer/NVRHI/PathTracePrimaryPass.cpp
    neo/renderer/NVRHI/PathTracePrimaryPass.h
    neo/renderer/NVRHI/PathTraceSmokeDispatch.cpp
    neo/renderer/NVRHI/PathTraceSmokeResources.cpp
    neo/shaders/CMakeLists.txt
    neo/shaders/builtin/pathtracing/cleanroom_rtxdi/pathtrace_clean_rtxdi_di_spatial.rt.hlsl
    docs/restir_remix_light_universe

Required:

    keep the existing sentinel temporal producer intact
    run spatial as a separate shader/pass after the temporal producer pass
    read neighbor reservoirs from an explicitly copied source page
    write spatial output to a distinct spatial reservoir page
    keep spatial default-on for view 12 and opt-out by cvar
    expose route-ready dump fields that prove the spatial shader and reservoir
        page are active
    keep view 8 band 16 as the spatial diagnostic

Forbidden:

    no RTXDI reservoir math changes
    no denoiser, confidence, gradient, or enhanced-paper replacement in this
        slice
    no mode-56 route
    no NEE-cache/ReGIR/PDFNEE replacement for the active RLU light universe
    no full-map/full-domain brute force or broad light suppression as the fix
    no claiming full RTX Remix spatial parity

Acceptance:

    PT-dev Release build compiles the separate clean DI spatial shader
    default clean DI view 12 dump reports spatial=1, selectedCleanShader=2,
        cleanSpatialReservoir=1, and pages spatial=u72
    view 8 band 16 shows neighbor-acceptance coverage
    r_cleanDiSpatial 0 returns to temporal-only behavior
        without changing RLU dense-domain counts or emissive replay eligibility
