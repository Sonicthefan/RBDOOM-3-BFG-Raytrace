Worker Tasks
============

Use these tasks in order. Do not skip ahead.


RDI-00: Remix Raw DI Contract Audit
-----------------------------------

Type:

    read-only audit

Goal:

    Produce the exact Remix raw DI initial + temporal contract for the mode
    where spatial, best lights, gradients, confidence, and denoising are off.

Reference files:

    E:/prog/references/dxvk-remix-git/src/dxvk/rtx_render/rtx_rtxdi_rayquery.cpp
    E:/prog/references/dxvk-remix-git/src/dxvk/shaders/rtx/pass/rtxdi/rtxdi_initial_sampling.comp.slang
    E:/prog/references/dxvk-remix-git/src/dxvk/shaders/rtx/pass/rtxdi/rtxdi_temporal_reuse.comp.slang
    E:/prog/references/dxvk-remix-git/src/dxvk/shaders/rtx/pass/rtxdi/rtxdi_reuse_bindings.slangh
    E:/prog/references/dxvk-remix-git/src/dxvk/shaders/rtx/algorithm/rtxdi/RtxdiApplicationBridge.slangh

rbdoom files to inspect:

    neo/renderer/NVRHI/PathTraceSmokeDispatch.cpp
    neo/renderer/NVRHI/PathTraceCVars.cpp
    neo/renderer/NVRHI/PathTraceCVars.h
    neo/renderer/NVRHI/PathTraceRemixRtxdiResourceGate.h
    neo/shaders/builtin/pathtracing/pathtrace_restir_combined_resolve.rt.hlsl
    neo/shaders/builtin/pathtracing/RtxdiBridge/RAB_LightLoadRuntime.hlsli
    neo/shaders/builtin/pathtracing/RtxdiBridge/RAB_LightSamplingCore.hlsli
    neo/shaders/builtin/pathtracing/RtxdiBridge/RAB_Visibility.hlsli
    neo/shaders/builtin/pathtracing/PathTracePrimarySurface.hlsli

Required output:

    Remix pass order for raw DI initial + temporal
    Remix feature toggles that must be off
    Remix input resource table
    Remix output resource table
    rbdoom equivalent resource table
    io_whitelist.txt allowed input/output mapping
    forbidden input classes that resemble existing rbdoom resources
    missing rbdoom adapters
    exact clean-room CVar implementation plan from cvar_contract.txt
    exact first implementation write set recommendation

Forbidden:

    no edits
    no build
    no code proposal beyond the contract table


RDI-01: New Clean-Room Path Scaffold
------------------------------------

Type:

    narrow implementation

Goal:

    Add a new isolated rbdoom clean-room DI path that can write a full-screen
    sentinel output and cannot fall back to existing RRX/smoke output.

Reference file-equivalent:

    E:/prog/references/dxvk-remix-git/src/dxvk/rtx_render/rtx_rtxdi_rayquery.cpp

Allowed rbdoom write set:

    new shader file(s) under neo/shaders/builtin/pathtracing/cleanroom_rtxdi/
    new C++ dispatch/resource file(s), if needed, under neo/renderer/NVRHI/
    neo/renderer/NVRHI/PathTraceCVars.cpp
    neo/renderer/NVRHI/PathTraceCVars.h
    neo/shaders/CMakeLists.txt
    minimal CVar declarations from cvar_contract.txt

Requirements:

    new path is not the existing RRX path
    new path is not activated through r_pathTracingDebugMode 56
    new path is controlled by r_pathTracingCleanRtxdiDiEnable and
        r_pathTracingCleanRtxdiDiView
    new path reads no forbidden input from io_whitelist.txt
    output writes every pixel
    sentinel color has no albedo, no GI, no direct lighting
    route can be enabled/disabled by a CVar
    build/deploy succeeds

Forbidden:

    no reservoir math
    no light sampling
    no temporal reuse
    no edits to existing synthetic patch logic
    no mode-56 route


RDI-02: Primary Surface Adapter Proof
-------------------------------------

Type:

    narrow implementation

Goal:

    Connect current primary surface input to the clean-room path and prove
    every pixel has explicit status output.

Reference file-equivalent:

    E:/prog/references/dxvk-remix-git/src/dxvk/shaders/rtx/algorithm/rtxdi/RtxdiApplicationBridge.slangh

Allowed rbdoom write set:

    clean-room DI shader files only

Requirements:

    valid opaque diffuse surface = one explicit color
    invalid surface = one explicit color
    unsupported surface = one explicit color
    no fallback lighting
    no reservoir logic yet

Forbidden:

    no light sampling
    no temporal reuse
    no BRDF/beauty shading


RDI-03: Analytic Light Adapter Proof
------------------------------------

Type:

    narrow implementation

Goal:

    Prove clean-room path can load and sample Doom analytic lights using the
    rbdoom light universe.

Reference file-equivalent:

    E:/prog/references/dxvk-remix-git/src/dxvk/shaders/rtx/algorithm/rtxdi/RtxdiApplicationBridge.slangh

Allowed rbdoom write set:

    clean-room DI shader files only

Requirements:

    Doom analytic light range is read
    selected analytic light identity is visible as hash color
    invalid/no-light state is loud
    targetPdf zero state is loud
    visibility accepted/rejected state is visible
    once a light sample is replayed into visible radiance, the worker must also
        provide a raw flat-diffuse analytic validation view:
            constant diffuse reflectance
            physically based direct-light estimate
            one sampled analytic light candidate per pixel
            no material albedo, textures, fallback lighting, denoiser, spatial,
                or best-light input

Forbidden:

    no reservoir temporal reuse
    no spatial
    no best lights
    no broad light suppression
    no fallback lighting


RDI-04: DI Initial Reservoir Producer
-------------------------------------

Type:

    narrow implementation

Goal:

    Build the first clean-room DI initial reservoir stage for Doom analytic
    lights only.

    This task is mandatory. It is the clean-room path's NEE-equivalent
    direct-light sampling stage. Workers must not skip it, replace it with old
    rbdoom NEE records, or consume old selected-light/direct-light output.

Reference file-equivalent:

    E:/prog/references/dxvk-remix-git/src/dxvk/shaders/rtx/pass/rtxdi/rtxdi_initial_sampling.comp.slang

RTXDI math reference:

    E:/prog/references/RTXDI-main/Libraries/Rtxdi/Include/Rtxdi/DI/InitialSampling.hlsli
    E:/prog/references/RTXDI-main/Libraries/Rtxdi/Include/Rtxdi/DI/Reservoir.hlsli

Allowed rbdoom write set:

    clean-room DI shader files
    clean-room DI resource/dispatch files only if needed

Requirements:

    current reservoir page is written
    current reservoir page is clean-room owned
    producer samples candidate lights from the current light universe
    producer evaluates targetPdf for the current surface
    producer optionally uses the rbdoom visibility helper if initial visibility
        is enabled
    every pixel writes either valid reservoir or explicit invalid state
    reservoir-only debug shows selected-light identity and fields
    raw flat-diffuse current-only validation view is mandatory:
        one sampled analytic light candidate per pixel
        constant diffuse reflectance
        physically based direct-light estimate from the current reservoir
        visibly noisy under a static camera
        no status colors used as accumulation proof
    no previous reservoir is read
    no previous-best input is used

Forbidden:

    no temporal reuse
    no spatial
    no best lights
    no synthetic reservoir builder
    no new weighting formula
    no old NEE selected-light records
    no old NEE reservoirs
    no old NEE direct-light contribution output


RDI-05: DI Temporal Reservoir Producer
--------------------------------------

Type:

    narrow implementation

Goal:

    Add clean-room DI temporal reuse over the RDI-04 current reservoir and
    previous frame reservoir page.

    RDI-05 is blocked unless RDI-04 produced an owned clean-room DI initial /
    NEE-equivalent current reservoir page.

Reference file-equivalent:

    E:/prog/references/dxvk-remix-git/src/dxvk/shaders/rtx/pass/rtxdi/rtxdi_temporal_reuse.comp.slang

RTXDI math reference:

    E:/prog/references/RTXDI-main/Libraries/Rtxdi/Include/Rtxdi/DI/TemporalResampling.hlsli
    E:/prog/references/RTXDI-main/Libraries/Rtxdi/Include/Rtxdi/DI/ReservoirStorage.hlsli

Allowed rbdoom write set:

    clean-room DI shader files
    clean-room DI dispatch/resource files only if page scheduling is required

Requirements:

    temporal input page is previous frame reservoir page
    temporal output page is current frame temporal page
    both pages are clean-room owned
    current reservoir is preserved when previous projection fails
    failed previous light translation rejects only previous sample
    debug output shows current-only vs temporal-reused
    temporal can be disabled to produce current-only noisy output
    raw flat-diffuse temporal validation view is mandatory:
        same flat diffuse resolve as RDI-04
        temporal off shows the current-only 1 spp noisy analytic result
        temporal on shows the same mean lighting with noise reduced over frames
        camera motion shows temporal tracking/reprojection behavior
        disocclusion and failed projection become current-only/noisy, not
            fallback lighting
    split-screen current-only vs temporal is strongly preferred for user
        validation

Forbidden:

    no spatial
    no best lights
    no fallback albedo
    no history brightness ramp
    no luminance averaging filter presented as temporal accumulation
    no false-color status dashboard presented as temporal accumulation
    no threshold tuning as a fix
    no previous-temporal-as-best


RDI-06: Reservoir-Only User Validation Build
--------------------------------------------

Type:

    build/deploy stop

Goal:

    Produce a runtime build for user visual testing of clean-room raw DI initial
    + temporal only.

Requirements:

    build succeeds
    exe copied
    shader contents copied without nesting
    hashes verified
    exact cvars provided
    user-facing validation instructions include:
        raw flat-diffuse current-only view
        raw flat-diffuse temporal view
        expected noisy-to-less-noisy behavior under a static camera
        expected reservoir tracking/reprojection behavior while moving
        exact statement that status/debug views are diagnostics only
    no commit until user confirms visual behavior

Stop:

    stop after build/deploy for user visual testing


RDI-07: Remix/RRX Temporal Surface Contract Recovery
----------------------------------------------------

Type:

    read-only audit first, then narrow implementation only after the boundary is
    proven

Goal:

    Recover clean view-12 temporal behavior by making the clean DI temporal
    surface/reprojection/visibility contract match RTX Remix and the local
    RRX/Remix-shaped temporal path closely enough for RTXDI temporal reuse and
    initial visibility to matter.

    This task is unlocked when RDI-06-style runtime testing proves that the
    clean current producer is active but real Doom analytic temporal output
    still does not visibly accumulate.

    Current evidence:

        r_pathTracingCleanRtxdiDiInitialVisibility reaches the shader and is
        reported by r_pathTracingCleanRtxdiDiDump as initialVisibility=1.
        It did not visibly improve clean view 12 with real Doom analytic
        lighting. In RTX Remix, the comparable Initial Visibility toggle has
        a large impact when temporal reuse is enabled. Therefore the blocker is
        likely not the toggle itself, but the clean temporal consumer/surface
        contract around it.

Reference file-equivalent:

    E:/prog/references/dxvk-remix-git/src/dxvk/shaders/rtx/pass/rtxdi/rtxdi_temporal_reuse.comp.slang

Additional reference files:

    E:/prog/references/dxvk-remix-git/src/dxvk/shaders/rtx/pass/rtxdi/rtxdi_initial_sampling.comp.slang
    E:/prog/references/dxvk-remix-git/src/dxvk/shaders/rtx/algorithm/rtxdi/RtxdiApplicationBridge.slangh
    E:/prog/references/RTXDI-main/Libraries/Rtxdi/Include/Rtxdi/DI/InitialSampling.hlsli
    E:/prog/references/RTXDI-main/Libraries/Rtxdi/Include/Rtxdi/DI/TemporalResampling.hlsli

rbdoom read set:

    neo/shaders/builtin/pathtracing/cleanroom_rtxdi/pathtrace_clean_rtxdi_di_sentinel.rt.hlsl
    neo/shaders/builtin/pathtracing/remix_rtxdi/rtxdi_temporal_reuse.rt.hlsl
    neo/shaders/builtin/pathtracing/remix_rtxdi/rtxdi_initial_sampling.rt.hlsl
    neo/shaders/builtin/pathtracing/remix_bridge/RAB_SurfaceBridge.hlsli
    neo/shaders/builtin/pathtracing/PathTracePrimarySurface.hlsli
    neo/shaders/builtin/pathtracing/RtxdiBridge/RAB_Visibility.hlsli
    neo/renderer/NVRHI/PathTraceSmokeDispatch.cpp

Allowed rbdoom write set after audit:

    neo/shaders/builtin/pathtracing/cleanroom_rtxdi/pathtrace_clean_rtxdi_di_sentinel.rt.hlsl
    neo/renderer/NVRHI/PathTraceSmokeDispatch.cpp
    neo/renderer/NVRHI/PathTraceCVars.cpp
    neo/renderer/NVRHI/PathTraceCVars.h

Audit requirements:

    Compare clean view 12 against Remix/RRX for:
        current surface loading and primary-surface field completeness
        previous surface loading and previous-pixel lookup
        motion-vector interpretation
        expected previous depth / hit-distance handling
        current reservoir read
        previous reservoir read
        selected light replay on the current surface
        selected light replay on the previous/temporal surface when required
        current-to-previous and previous-to-current light mapping
        initial selected-sample visibility
        temporal selected-sample visibility / discard policy

    Identify the first exact contract boundary:
        A. clean current reservoir is already bad
        B. clean current reservoir is valid but visibility/PDF/replay is bad
        C. previous surface or motion-vector remap differs from Remix/RRX
        D. previous reservoir/light translation differs from Remix/RRX
        E. final resolve differs after a valid temporal reservoir

Implementation requirements:

    Keep the RLU-backed current light domain.
    Keep clean current reservoir ownership.
    Reuse shared rbdoom surface/visibility adapters where they already match
        the RRX/Remix-shaped path.
    Preserve r_pathTracingCleanRtxdiDiInitialVisibility as a diagnostic/parity
        switch.
    Preserve r_pathTracingCleanRtxdiDiView8Band 8 as the current-vs-temporal
        selected-sample failure classifier.
    Provide a split current-vs-temporal raw flat-diffuse validation view.

Forbidden:

    no spatial
    no best lights
    no full-map/full-domain brute force as a fix
    no broad light suppression
    no target-floor or BRDF-gate diagnostic hack as a fix
    no threshold tuning before the surface/reprojection contract is matched
    no old previous-temporal-as-best fallback
    no replacing RLU with old Doom analytic portal-domain producers
    no claims from status colors, dumps, buffer survival, or non-crashing builds

Stop:

    Stop after build/deploy for user visual testing. Do not commit until the
    user confirms visible noisy-to-less-noisy temporal accumulation on real
    Doom analytic lighting.


Deferred Tasks
--------------

Do not start before RDI-06 user confirmation:

    emissive triangle adapter
    spatial reuse
    best-light feedback
    gradients/confidence
    denoiser integration
    beauty-output integration
