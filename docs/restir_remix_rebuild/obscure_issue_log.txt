Obscure Issue Log
=================

Purpose
-------

This file records semi-obscure ReSTIR/RTXDI/Remix rebuild issues that are worth
preserving for future agents, but do not currently justify a dedicated fix
slice. These notes are guardrails. Do not treat them as permission to widen a
worker task.


RRX-05A: Conservative Visibility Callback Names
-----------------------------------------------

Status:

    Recorded only. No required fix while DI initial visibility remains deferred.

Files:

    neo/shaders/builtin/pathtracing/remix_rtxdi/rtxdi_initial_sampling.rt.hlsl

Issue:

    Rtxdi/DI/InitialSampling.hlsli expects RAB conservative visibility callback
    symbols to exist. The inactive rbdoom Remix DI initial shader currently
    keeps RAB_GetConservativeVisibility callbacks backed by a facing/geometry
    test.

Risk:

    Future agents may mistake these callbacks for RTX Remix explicit selected
    light visibility and route them into the initial-visibility path. That would
    silently reintroduce the earlier bug where a geometry-facing test is treated
    as real shadow visibility.

Current safe state:

    RRX-05A defers initial visibility through RemixRtxdiDeferInitialVisibility.
    The conservative callbacks are therefore callback-compatibility scaffolding,
    not the active visibility implementation.

Preferred future handling:

    Keep the callbacks only as RTXDI symbol compatibility until a real rbdoom
    visibility/shadow bridge exists. If a later slice enables explicit initial
    visibility, it must call a real selected-light visibility path equivalent to
    Remix RTXDI_UpdateSelectedReservoirVisibility behavior, not this geometry
    test.

Do not:

    remove the callbacks without checking RTXDI header compile requirements
    use the callbacks as proof that explicit visibility is implemented
    convert the facing/geometry test into reservoir visibility

view 68 quirks

    View 68 writes a clean RRX DI current-page seed from the active Remix/RAB light manager before temporal reuse. If no valid
    RRX DI initial seed can be produced, the view reports local rejection instead of fabricating input from old PT/spatial output.
    The view-68 current seed follows the Remix-shaped initial sampling pattern: frame-indexed RTXDI RNG, strided traversal of
    the active light-manager range, random local-light UVs, all candidates streamed, and one final RTXDI_FinalizeResampling call.
    When reprojection is not eligible, the active temporal path keeps the current RRX DI seed as current-only output instead
    of calling the temporal core with zero motion or clearing the reservoir.
    The left half is now split into five vertical cause bands: projected, previous sample used, previous-to-current
    translation valid, current-to-previous translation valid, and selected-light stability. Green means pass/stable, red
    means failed, blue means no previous sample, purple means previous remap invalid, and dark means not applicable.
    Right-half green still visualizes output M/history as a numeric heat channel.
    PathTraceRemixRtxdiResourceGate.* is only the resource/clear predicate owner. It is not a standalone Remix RTXDI
    temporal dispatch boundary.
    The RRX DI clear gate must include normal mode 56 when r_pathTracingRemixRtxdiResourcesEnable is set; otherwise the
    initial clearPending state keeps RestirPTRrxDiReservoirAvailable false and prevents the active consumer from starting.
    Normal mode 56 now consumes the RRX DI temporal reservoir as the direct-lighting contribution when that path produces a
    valid reservoir, and falls back to the old spatial direct-lighting path only when RRX DI is unavailable or invalid.
    The RRX DI consumer is still rbdoom's local preview/final resolve bridge, not full Remix DI parity. It applies rbdoom
    preview visibility via TraceSmokeShadowVisibility, not the RAB selected-light visibility callback or reference-equivalent
    final-shading contract.


limited impact fix later
P2: The contribution path still uses rbdoom-local visibility via TraceSmokeShadowVisibility, not RAB_GetSelectedNeeVisibility.
 It is documented in obscure_issue_log.txt, so I would not block testing on it, but don’t call this full Remix/RAB final-shading parity yet.
P2: r_pathTracingRemixRtxdiResourcesEnable now changes output behavior, not just resource ownership.
That is okay for this experimental path, but the cvar name is misleading if it survives beyond the test slice.
A later cleanup should split allocate-RRX-resources from use-RRX-DI-for-mode-56-direct-lighting.


Portal RTX Best-Light And Initial Visibility Observation
-------------------------------------------------------

Status:

    Recorded only. This is useful behavior, but current Portal RTX testing says
    it is not the source of reservoir persistence.

Reference locations:

    E:/prog/references/dxvk-remix-git/src/dxvk/rtx_render/rtx_rtxdi_rayquery.cpp
    E:/prog/references/dxvk-remix-git/src/dxvk/rtx_render/rtx_rtxdi_rayquery.h
    E:/prog/references/dxvk-remix-git/src/dxvk/shaders/rtx/pass/rtxdi/rtxdi_initial_sampling.comp.slang
    E:/prog/references/dxvk-remix-git/submodules/rtxdi/rtxdi-sdk/include/rtxdi/ResamplingFunctions.slangh
    E:/prog/references/dxvk-remix-git/submodules/rtxdi/rtxdi-sdk/include/rtxdi/Reservoir.slangh

Observation:

    Portal RTX's Lighting -> RTXDI -> Sample Best Lights checkbox controls
    rtx.di.enableBestLightSampling. When disabled, the shader-side
    previous-best-light path is not consumed by initial sampling or temporal
    reuse, although the gradient pass may still write the best-light texture if
    denoiser gradients are enabled.

    The checkbox below it, Initial Visibility, controls
    rtx.di.enableInitialVisibility. Remix defines RTXDI_DEFER_INITIAL_VISIBILITY
    for the initial sampling shader, so it does not trace every random/best
    light candidate. It first chooses the initial reservoir winner, then traces
    one visibility ray to that selected light through
    RTXDI_UpdateSelectedReservoirVisibility. If the selected sample is fully
    invisible, RTXDI_StoreVisibilityInReservoir invalidates only that selected
    reservoir sample by setting the light index invalid and weight sum to zero.

User test result:

    With Portal RTX, disabling best-light sampling did not make the accumulator
    give up or flush reservoirs. It did reduce quality in sparse areas. In one
    sparse traced area, best-light sampling produced noticeably more useful
    samples only when Initial Visibility was also enabled.

Interpretation:

    Best-light sampling plus selected-light initial visibility is a quality and
    candidate-validation feature. It is not the mechanism that prevents broad
    reservoir clearing. Reservoir persistence appears to come from Remix's
    ownership/invalidation architecture: dynamic light/object churn updates
    payloads and mappings, while failed visibility or failed remap rejects only
    the selected candidate/sample locally.

Guardrail:

    Do not use Portal RTX stability with best-light disabled as evidence that
    rbdoom can skip local selected-sample visibility forever. Also do not chase
    best-light sampling as the primary fix for global reservoir flushes. Broad
    clears tied to old smoke signatures, dynamic-light payload churn, or scene
    generation remain nonconformant with the observed Remix behavior.
