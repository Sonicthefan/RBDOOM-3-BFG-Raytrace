RBDoom3 BFG RT/PT handoff
=========================

This document is for a fresh Codex or human contributor picking up the
experimental Vulkan/NVRHI RT smoke/path tracing branch without reading the full
chat history.

Current snapshot - 2026-05-09
-----------------------------

Repository:

    E:\prog\rbdoom-3-bfg-rt

Branch:

    codex/pathtrace-core-refactor-plan

Prepared game folder used for testing:

    E:\prog\rbdoom-3-BFG-prebuilt

Latest mode 18 NEE state:

- The first native mode 18 NEE include now exists at
  `neo/shaders/builtin/pathtracing/pathtrace_nee.hlsli`.
- `pathtrace_smoke.rt.hlsl` still owns the monolithic raygen, but mode 18
  selected-light and Doom analytic direct-light helpers are now behind the NEE
  include boundary.
- The NEE structs now carry native extension points for source type/index,
  surface material/instance/primitive identity, light-selection/sample/target
  PDFs, target weight, visibility, MIS placeholder, and path-event
  classification. This is a local contract only; it does not import RTXDI code
  or enable temporal/spatial reuse.
- `r_pathTracingSecondaryNeeMode` controls secondary selected-light NEE:
  `0` = off/diagnostic throttle, `1` = one uniformly sampled selected point
  light with inverse-PDF compensation, `2` = legacy full selected-light loop.
- `r_pathTracingSecondaryNeeVisibility` defaults to `1` and controls only the
  sampled secondary selected-light shadow visibility ray. Set it to `0` for
  diagnosis only, to shade the selected sample as visible and isolate whether
  remaining instability comes from the visibility ray path.
- `r_pathTracingNextEventEstimation 0` remains the global NEE off switch.
- Primary selected-light direct lighting still uses the legacy full selected
  light loop for comparison. Doom analytic lights remain under their existing
  analytic-light CVars and still use the legacy analytic loop.
- Debug and Release/PT-dev builds passed after the extraction. The Release/PT
  dev executable and updated DXIL/SPIR-V `pathtrace_smoke.rt.bin` files were
  copied to `E:\prog\rbdoom-3-BFG-prebuilt`.
- User runtime test with analytic lights off: the game launched and ran without
  startup errors. The sampled secondary NEE path seems somewhat better and
  crashes less often, but there are still issues. Analytic lights are still too
  heavy to keep enabled for this validation. Do not treat the NEE extraction as
  a complete device removal/stability fix.
- Follow-up runtime test with analytic lights still off: comparing
  `r_pathTracingSecondaryNeeVisibility 0` and `1` showed roughly a 50% RT speed
  difference. The recent NEE changes may move the texture-corruption/device
  crash threshold somewhat higher in resolution, but do not eliminate it. This
  points at sampled selected-light shadow visibility rays and/or the broader
  monolithic raygen/resource-pressure path as the next diagnostic target, not
  analytic-light fan-out.
- Further user observation: stripping path-tracing features with CVars mostly
  delays the resolution at which texture corruption/device removal appears,
  from around 720p/3SPP in the heavier path up toward 2500x2000 with more work
  disabled. Treat this as a resolution/workload pressure threshold, not as proof
  that any one stripped feature is the sole root cause.
- Additional user observation: the corruption/crash is sensitive to portal
  traversal thresholds. The typical pattern is texture corruption followed by a
  crash when portal thresholds are crossed. Treat portal-area selection changes,
  source3 residency/light-area set churn, and resource/descriptor lifetime
  around those transitions as first suspects for the next diagnostic slice.
- Follow-up nuance: there is also chronic texture corruption before the
  portal-threshold crash. The chronic corruption and portal-crossing crash may
  be two separate failure modes that both become visible once GPU load is high
  enough to push FPS down. Do not assume portal traversal is the only cause of
  corruption; treat it as one high-signal trigger to instrument alongside
  general high-load texture/resource corruption.
- Prior texture-off test: disabling real texture sampling reduced load, like
  other feature stripping, but placeholder surfaces still eventually glitched
  once rays or resolution were high enough. This argues against ordinary
  material texture sampling or bad texture contents as the sole cause; keep RT
  output/accumulation/history resources, descriptors, synchronization, and
  monolithic shader/driver pressure in the suspect set.
- Portal-depth reduction test: setting the main render-affecting portal depths
  to 1 (`r_pathTracingRigidResidencyPortalSteps 1`,
  `r_pathTracingStaticAreaPreloadPortalSteps 1`,
  `r_pathTracingLightAreaPortalSteps 1`) still glitched and crashed easily.
  Portal threshold crossings can still trigger failures, but traversal depth is
  not the main stabilizing lever.
- Task 03 NEE contract cleanup runtime result: no new problems observed after
  adding the native source/PDF/visibility/MIS/path-event extension fields, but
  no stability or performance improvement either. Treat that slice as a clean
  contract-preparation step, not a fix for the high-load corruption/device
  removal issue.
- New portal/resource-lifetime diagnostic slice: `PathTraceSmokeResources`
  can now detect committed PT scene transitions across scene source,
  view/current area, selected portal counts, geometry/material/light counts,
  scene signatures, descriptor table create/write state, binding-set changes,
  buffer handle replacement, and static/dynamic BLAS handle replacement.
  `r_pathTracingPortalTransitionDump 1` prints the next portal-area transition
  and then auto-disables; `2` prints every portal-area transition; `3` prints
  every broad scene/resource transition and is expected to flood the console.
  `r_pathTracingWaitForIdleOnPortalChange 1` waits for the NVRHI device to idle
  immediately before replacing committed PT scene resources on broad scene
  transitions, and now runs silently unless dumping is separately enabled. This
  is diagnostic only: if it stops portal-threshold texture corruption/device
  removal, the real fix should be frame-retired PT scene resources or
  fence-based retirement, not a permanent idle.

Recommended next validation:

    r_pathTracingAnalyticLightCandidates 0
    r_pathTracingDisableAnalyticLightLoop 1
    r_pathTracingSamplesPerPixel 3
    r_pathTracingMaxPathDepth 2
    r_pathTracingNextEventEstimation 1

Then compare:

    r_pathTracingSecondaryNeeMode 0
    r_pathTracingSecondaryNeeMode 1
    r_pathTracingSecondaryNeeMode 2
    r_pathTracingSecondaryNeeVisibility 0
    r_pathTracingSecondaryNeeVisibility 1
    r_pathTracingDisableDiffuseSecondaryRay 1
    r_pathTracingDisableSelectedLightLoop 1

For portal/resource lifetime diagnosis, run the same baseline with:

    r_pathTracingPortalTransitionDump 1
    r_pathTracingWaitForIdleOnPortalChange 0

This captures one portal-area transition and auto-disables so the console is not
flooded. Then repeat the portal-threshold movement test with:

    r_pathTracingWaitForIdleOnPortalChange 1

If the wait-for-idle variant removes the corruption/crash, prioritize a real
scene-resource retirement queue for old buffers, descriptor tables, binding
sets, and BLAS/TLAS handles. If it does not help, keep the transition logs and
move to descriptor reuse, acceleration rebuild, and monolithic shader pressure
diagnostics without treating portal scene replacement as proven.

If mode 1 is still unstable with analytic lights off, keep the next slice
diagnostic and narrow, but stop treating CVar stripping as a root-cause finder
once it only moves the resolution threshold. The next code slice should target
resolution/workload-scaled resource lifetime, descriptor/resource reuse,
barrier/synchronization, or monolithic raygen pressure. Portal-threshold
scene/light churn remains a useful trigger to instrument, but reducing portal
depth to 1 did not stabilize the issue. The `SecondaryNeeVisibility 0/1` result
still means visibility rays are a major cost multiplier, but the broader
threshold behavior means they may only be exposing one underlying failure
earlier. Keep the diagnostics able to distinguish chronic high-load texture
corruption from the portal-threshold crash. Treat sampled analytic-light NEE as
a later follow-up. Do not jump to full RTXDI/ReSTIR PT, ReGIR, material
redesign, geometry/BVH changes, or a separate C++ pass unless the evidence
specifically demands it.

Historical snapshot - 2026-05-06
-----------------------------

Repository:

    E:\prog\rbdoom-3-bfg-rt

Branch:

    codex/refactor-pathtrace-smoke-modules

Prepared game folder used for testing:

    E:\prog\rbdoom-3-BFG-prebuilt

Current operational state:

- The BVH/scene-source emergency is over. Do not start another broad BVH rewrite
  or portal-system intervention unless a new, concrete regression proves the
  current source3 stack is insufficient.
- The source3 portal-resident BVH stack is now the default baseline for normal
  PT smoke testing. Mode 18, mode 20, and the BVH diagnostic modes should work
  without entering a long list of setup CVars after game launch.
- The old drawSurf-visible-only BVH path is deprecated but intentionally kept as
  an emergency comparison path.
- Release/PT build passed after the source3-default change and the executable
  was copied to the prepared game folder.

Current default scene/BVH policy:

- `r_pathTracingSceneSource` defaults to `3`.
- Static world geometry uses source3 area-selected preload.
- Rigid objects use local-space rigid BLAS plus TLAS transforms.
- Rigid residency keeps route-ready rigid instances resident beyond raster
  visibility when their renderer area remains in the selected portal set.
- Static preload, rigid residency, and light-area selection all default to a
  current-area plus four portal-step walk. Current+1 was tested and is too
  conservative for Doom 3; many visually single rooms span several renderer
  portal areas.
- The implementation uses Doom renderer area/portal membership as the RT
  residency unit. The relevant renderer APIs are `PointInArea`,
  `NumPortalsInArea`, and `GetPortal`. Do not substitute raw BSP leaf/internal
  nodes as the residency unit.
- `PS_BLOCK_VIEW` portal edges are currently counted/reported but do not hard
  stop source3 static preload, rigid residency, or light-area traversal. Hard
  blocking reproduced missing-BVH behavior in user tests.

Important defaults now expected at startup:

    r_pathTracingSceneSource 3
    r_pathTracingRigidBlasGpuScaffold 1
    r_pathTracingRigidBlasGpuBuild 1
    r_pathTracingRigidTlasRoute 1
    r_pathTracingRigidRouteMode18 1
    r_pathTracingRigidRouteMode20 1
    r_pathTracingRigidRouteRemoveDynamic 1
    r_pathTracingRigidRouteEmissiveCards 1
    r_pathTracingRigidRouteMaxInstances 256
    r_pathTracingRigidResidency 1
    r_pathTracingRigidResidencyPortalSteps 4
    r_pathTracingStaticAreaPreload 1
    r_pathTracingStaticAreaPreloadPortalSteps 4
    r_pathTracingLightAreaPortalSteps 4

Emergency fallback if source3 needs comparison:

    r_pathTracingSceneSource 0
    r_pathTracingRigidTlasRoute 0
    r_pathTracingRigidResidency 0
    r_pathTracingStaticAreaPreload 0
    r_pathTracingRigidRouteRemoveDynamic 0

Testing baseline now:

- Use Release/PT dev builds only for meaningful PT behavior/performance tests.
- Normal runtime checks can start from just selecting the debug mode:

      r_pathTracingDebugMode 18
      r_pathTracingDebugMode 20
      r_pathTracingDebugMode 21
      r_pathTracingDebugMode 22

- Mode 18 is the stable/playable toy one-bounce path trace path.
- Mode 20 is still single-frame reservoir direct emissive lighting. Spatial and
  temporal reuse are not implemented.
- Modes 21/22 are solid/wireframe BVH bounds diagnostics tied to the current
  source3 scene stack, useful for quickly spotting residency/culling mistakes.
- `r_pathTracingWorldStaticEmissives` must remain default `0`.
- `r_pathTracingDynamicOccluderRadius` must remain default `0`.
- `r_pathTracingSkipRaster3D` defaults to `1` and should stay that way unless
  deliberately testing raster interaction.

Current known caveats:

- Wider campaign testing is limited because unrelated game-logic parity work is
  still incomplete outside the opening section.
- Moving/off-screen resident rigid objects currently use last observed
  transform/material metadata until they are seen again. This is acceptable as a
  baseline but is not final motion-vector or animation handling.
- Rigid emissive-card promotion is conservative. It rejects GUI/particle-like,
  glass, post-process, skinned/deforming, callback/generated, depth-hack,
  dynamic-model, and `_add + 0200` cases. A temporary `swinglight` exclusion
  remains because that family is game-logic/register driven and must stay on the
  dynamic drawSurf path until a general dynamic-register/material-parm detector
  exists.
- Light-area filtering diagnostics exist, but light-area filter application is
  not the main default safety mechanism. The source3 portal-resident scene stack
  is the important baseline.
- Do not reintroduce the unsafe routed rigid `triangleClassAndFlags` GPU payload
  attempt. It broke broad surface rendering and was reverted.

Resolved PT output resize crash - 2026-05-08
--------------------------------------------

Changing `r_pathTracingDebugWidth` or `r_pathTracingDebugHeight` caused a hard
GPU device-removal crash after the console printed the new PT output
resolution. The application/window back-buffer resize path was a false lead:
the PT render resolution is controlled by those debug CVars, not by the game
window size. A transient aspect-ratio mismatch from changing width and height
one at a time was also not the root cause.

The actual issue was PT internal output-size resource lifetime. When the debug
width/height changed, `ResizeRayTracingSmokeOutput` replaced output,
accumulation, readback, smoke reservoir, and ReSTIR PT reservoir resources while
the previous frame could still have RT dispatch, blit, readback copy, or
descriptor use in flight. The fix is to call `waitForIdle()` and
`runGarbageCollection()` in `PathTraceSmokeResources::ResizeRayTracingSmokeOutput`
before replacing existing output-sized PT resources. Keep that fence at the PT
resource owner. Future app/native back-buffer resize work should not be
confused with PT debug output-size changes.

Current ReSTIR PT bridge state - 2026-05-08
-------------------------------------------

ReSTIR PT integration has completed the first ten bridge steps:

- Step 1: separate host-side packed ReSTIR PT reservoir allocation in
  `PathTraceRestirPTReservoirs.*`. It uses `RTXDI_PackedPTReservoir`,
  RTXDI reservoir addressing parameters, and the stock two PT reservoir slices.
- Step 2: pass-owned `rtxdi::ReSTIRPTContext` and `RTXDI_PTParameters` upload
  plumbing in `PathTraceRestirPT.*`, with resampling mode still forced to
  `ReSTIRPT_ResamplingMode::None`.
- Step 3: shader ABI binding only. The smoke ray tracing binding set now binds
  `RTXDI_PTParameters` at b28 and
  `RWStructuredBuffer<RTXDI_PackedPTReservoir>` at u29. The shader defines
  `RTXDI_PT_RESERVOIR_BUFFER` and compiles `Rtxdi/PT/Reservoir.hlsli`. No
  RTXDI pass, RAB bridge, reservoir load/store call, temporal reuse, or spatial
  reuse is enabled yet.
- Step 4: local shader-side RAB type declarations in
  `neo/shaders/builtin/pathtracing/RtxdiBridge`. These are RBDoom3-owned bridge
  files placed next to the smoke path tracing shader, not copied NVIDIA sample
  bridge files. They currently define only RAB_Material, RAB_Surface,
  RAB_RayPayload, RAB_LightInfo, RAB_LightSample, and RAB_RandomSamplerState
  plus small empty/accessor helpers.
- Step 5: `pathtrace_smoke.rt.hlsl` now has current-hit bridge helpers:
  `RAB_BuildMaterialFromSmokePayload` and `RAB_BuildSurfaceFromSmokePayload`.
  They translate an actual smoke ray payload into RAB material/surface data
  using the existing smoke material table, albedo sampling, normal-map decode,
  alpha coverage, emissive sampling, and specular helpers. The screen callback
  entry points `RAB_GetGBufferSurface` and `RAB_GetGBufferMaterial` exist, but
  intentionally return invalid data for both current and previous frame requests
  because the smoke shader still has no real current/previous primary-surface
  buffers or motion vectors. This avoids the dangerous shortcut of treating the
  current ray hit as previous-frame history. No visibility tests, BRDF
  evaluation callbacks, light sampling callbacks, RTXDI shader pass,
  temporal reuse, spatial reuse, or RAB_PathTrace implementation is enabled yet.
- Step 6: `RAB_RandomSamplerState.hlsli` now exposes local
  `RAB_RandomSamplerState`, `RAB_InitRandomSampler`, direct-seed construction,
  and `RAB_GetNextRandom`. The implementation wraps RTXDI's own random sampler
  helpers rather than introducing a separate weak generator. Future path replay
  should store and restore the same seed/index pair consumed by these callbacks.
- Step 7: `RAB_Surface.hlsli` now exposes conservative BRDF callbacks:
  `RAB_GetSurfaceBrdfSample`, `RAB_GetSurfaceBrdfPdf`,
  `RAB_EvaluateSurfaceBrdf`, `RAB_EvaluateSurfaceBrdfOverPdf`, and RTXDI-style
  alias names for BRDF sampling/PDF. This first implementation is deliberately
  opaque diffuse only: cosine-hemisphere sampling, matching solid-angle PDF,
  Lambert BRDF evaluation, and BRDF*cos/PDF throughput. Invalid, translucent,
  and special surfaces return no sample or zero contribution until real BSDF
  support is added.
- Step 8: light bridge callbacks now exist. `RAB_LightInfo.hlsli` loads
  current-frame smoke emissive triangles and Doom analytic sphere lights from
  the existing shader buffers. `RAB_LightSampling.hlsli` samples those two light
  types, computes light direction/distance, computes an opaque-diffuse target
  weight for a surface, and exposes a conservative visibility callback that
  only rejects invalid surfaces/samples. `RAB_MISCallbacks.hlsli` provides the
  expected MIS callback names, but the implementation is intentionally neutral
  until `RAB_PathTrace` can keep NEE and BRDF sampling accounting consistent.
  Previous-frame light loading returns empty data, and environment sampling is
  still unsupported.
- Step 9: a compile-validated `RAB_PathTrace` skeleton now exists under
  `neo/shaders/builtin/pathtracing/RtxdiBridge/PathTracer`. It compiles RTXDI's
  initial-sampling path tracer context against the local RAB bridge and wraps
  the existing smoke TLAS path: sample the conservative opaque-diffuse RAB BRDF,
  set `RTXDI_BrdfRaySample`, set a continuation ray, let RTXDI analyze
  reconnectibility, trace `SmokeScene`, convert the `PathTraceSmokePayload` back
  into RAB payload/surface data, record the intersection, and record emissive
  surface hits. The skeleton is capped to a small bridge bounce depth and still
  omits NEE, environment radiance, Russian roulette, PSR/denoiser state, traced
  RAB visibility, temporal replay, and spatial reuse. No RTXDI shader pass
  dispatches it yet.
- Step 10: `r_pathTracingDebugMode 26` is now a gated RTXDI initial-sampling
  reservoir writer/debug view. The smoke raygen traces the normal primary ray,
  converts the hit to a current-frame RAB surface, initializes an RTXDI
  path-tracer random context from the ReSTIR PT frame index, calls
  `GenerateInitialSamples`, and stores the returned `RTXDI_PTReservoir` into
  `RestirPTParams.bufferIndices.initialPathTracerOutputBufferIndex` with
  `RTXDI_StorePTReservoir`. `RAB_PathTrace` records one conservative NEE sample
  from the current RAB light list per visited path vertex before BRDF
  continuation, because the first BRDF-only probe was visually unlit/sparse in
  normal Doom scenes. The first single uniform light pick was still too weak, so
  mode 26 now uses the existing weighted emissive-triangle distribution and
  keeps the best local target from a small trial set. This was not caused by
  roughness/PBR conversion: mode 26 remains diffuse-only with roughness 1.0,
  which is acceptable for this reservoir visibility check. Misses store an empty
  reservoir and render black; GUI surfaces store empty and use the existing GUI
  composite; unsupported translucent/special surfaces store empty and render dim
  albedo. The visible output is only a heat/debug view of selected useful
  reservoirs. It does not consume reservoirs for final shading, does not enable
  temporal or spatial reuse, does not use previous-frame G-buffer history, does
  not trace final RAB visibility, and does not change modes 18/20. Mode 26 now
  participates in rigid TLAS routing, rigid residency, and routed rigid
  emissive-card inventory so it validates against the same source3
  scene-membership baseline as modes 18/20 instead of a culled/fallback subset.
  A temporary diagnostic fallback color key is active when RTXDI does not select
  a useful reservoir: orange means the shader found a nonzero local RAB light
  target, blue means valid light samples existed but contributed zero to the
  current surface, red means sampled light records were invalid, and purple
  means no light candidates reached the shader.
- Mode 27 is paired with mode 26 as an initial-reservoir shading preview. It
  runs the same initial sampling and source3 scene-membership path, writes the
  initial reservoir slice, and displays `TargetFunction * WeightSum` with simple
  tonemapping instead of mode 26's diagnostic color key. It is deliberately not
  Step 11 temporal reuse; previous primary-surface history and motion vectors
  are still missing from the smoke shader. User validation reported that mode 27
  works fairly well, runs well performance-wise, and has no serious stability or
  gameplay issues. Remaining rough normals and a few black objects are local RAB
  material/normal/unsupported-surface cleanup, not a source3 scene/light
  membership or initial-reservoir generation failure.

Shader changes were made, so test builds must copy `base/renderprogs2` into the
prepared game folder along with the executable.

Likely next task: Doom lights as PT analytic candidates
------------------------------------------------------

The user is considering the next work chunk: a basic attempt to leverage Doom 3
light entities as more physical PT light candidates. This is not a BVH emergency
task. Treat it as a light-universe / sampling scaffold.

Relevant reasoning:

- Doom 3 maps contain many authored lights. Brute force direct lighting that
  evaluates every relevant shadowed light per shaded hit scales poorly:

      visible hits * lights evaluated * shadow rays

- ReSTIR DI/PT is a good long-term fit for Doom 3 because many lights can become
  candidates while the shader traces only a small number of final visibility
  rays.
- A finite analytic light is a better first physical approximation than the
  current physically impossible point-light behavior. Start with analytic sphere
  lights or simple analytic area records, not triangle spheres inserted into the
  BVH.
- Analytic Doom lights do not need to be BVH geometry for direct lighting. They
  need stable IDs, area membership, sampling code, intensity/radius parameters,
  and shadow rays through the existing source3 BVH.
- Real emissive surfaces/signs/screens remain geometry/emissive candidates.
  Doom helper lights that duplicate emissive surfaces may eventually need an
  explicit map-scoped blacklist or PT overlay file.

Recommended first production slice:

1. Add diagnostics for Doom renderer lights before changing lighting output.
   Useful fields: map name, render light index, entity name if accessible,
   shader/material name, origin, radius/bounds, color, area number, active/on
   state, and portal-depth classification from the current player area.
2. Add a nearest/crosshair/player-proximity light dump so the tester can stand
   near problematic doubled lights and capture stable blacklist candidates.
3. Add an analytic light candidate record path behind a CVar. Prefer a separate
   CPU/GPU buffer for analytic light candidates rather than BVH geometry.
4. Start with sphere-light sampling in a diagnostic mode or mode-20-adjacent
   path. Keep the existing point-light behavior available for comparison.
5. Add intensity/radius CVars. Initial conversion can match the existing Doom
   point-light brightness approximately at a calibration distance, not attempt
   physical correctness from day one.
6. Keep candidates area-aware using the existing renderer area/portal walk.
   Default to the established four portal steps unless the task has a concrete
   reason to differ.

Do not do this first:

- Do not add triangle sphere meshes to the TLAS as the first light conversion.
- Do not disable or rewrite existing Doom lights globally.
- Do not auto-blacklist duplicate-looking lights without diagnostics and user
  confirmation.
- Do not treat this as a reason to revisit the source3 BVH foundation.

Potential follow-up systems:

- Map-scoped PT overlay files for analytic lights and Doom-light overrides, e.g.
  `base/pt_overlays/game/mars_city1.ptlights`.
- Map-scoped light blacklist keyed by stable identity:
  map + entity/renderLight index + shader + origin/radius.
- ReSTIR DI candidate reservoir over emissive triangles, rigid emissive cards,
  analytic Doom lights, and future overlay lights.
- Later solid-angle sphere sampling; uniform sphere-surface sampling is simpler
  for the first debug scaffold.

Recommended next small chunks, not urgent BVH intervention:

- Replace the temporary `swinglight` material-name exclusion with a real
  material/register dependency detector for shaderParm/time/sound/global-driven
  emissive stages.
- Add freshness/motion handling for moving resident rigid instances and future
  motion-vector requirements.
- Continue improving material classification in small, testable passes.
- Improve mode 20 light candidate quality/performance and eventually add
  temporal/spatial reuse.
- Continue unrelated game-logic parity work needed for broader campaign testing.

Recent source3/BVH-related commits:

    3af06460 pt: make source3 portal BVH default
    bb0a1abf pt: add source3 portal residency stack
    2c650586 docs: update path tracing routed rigid notes
    87254b9d pt: add light area filter impact diagnostics
    7c34b687 pt: add mode 20 test preset
    02e4a524 pt: route rigid BLAS instances into smoke modes
    3c583342 pt: add drawsurf mirror bounds diagnostics
    0ea87716 docs: add scene producer replacement guide
    296f3f5b pt: add static scene universe diagnostics

Current documentation map:

- `docs/pathtracing.txt` is the durable primary status/chronology document.
- `docs/pathtrace_modules.txt` is the durable file-by-file module map.
- `docs/pathtrace_geometry_universe.md` explains persistent geometry and
  source3 area/portal residency details.
- `docs/pathtrace_material_universe.md` explains persistent material facts and
  stable material indexes.
- `docs/pathtrace_interactive_surfaces.md` explains interactive/runtime material
  state and rigid emissive-card caveats.
- `docs/pathtrace_scene_producer_*` files are historical intervention/build-log
  records. They are useful context, but they are not the active roadmap and
  should not override the primary documents above.

Historical snapshot - 2026-05-04
-----------------------------

Repository:

    E:\prog\rbdoom-3-bfg-rt

Branch:

    codex/refactor-pathtrace-smoke-modules

Prepared stable/playable checkout:

    E:\prog\rbdoom-3-bfg-rt-stable

Prepared game folder used for testing:

    E:\prog\rbdoom-3-BFG-prebuilt

External references:

    E:\prog\references\RTXDI-main
    E:\prog\references\Q2RTX-master
    E:\prog\references\Q2RTX-rayreconstruction-master
    https://iddevnet.dhewm3.org/doom3/materials.html
    https://iddevnet.dhewm3.org/doom3/guis.html

Important worktree rules:

- NVIDIA RTXDI, RTXPT, Donut, and Q2RTX should be treated as external
  references only unless there is a deliberate clean integration plan.
- The old failed in-tree RTXPT/Donut attempt was removed. Do not re-add
  neo/extern/RTXPT, neo/renderer/NVRHI/RTXPT, or local RTXPT include wiring as
  drive-by ReSTIR/PT prep.

- Do not stage or commit this handoff file unless the user explicitly asks.

Current git state at handoff update time:

    Recent local committed work includes:

        33b52a18 Document interactive emissive surface behavior
        d40fe9fa Move static geometry lookup into universe
        ddc2ec19 Update path tracing docs for geometry universe
        e55ba1ab Add path trace geometry universe scaffold
        9a78a170 Cache universe material table work
        90719330 Default to material universe table
        d8e6ca22 Add universe-derived material table path
        7004b522 Add stable material universe indexes

    Run `git status -sb` before staging anything. At the time this handoff was
    updated, the old in-tree RTXPT/Donut attempt had been removed from
    neo/extern/RTXPT, neo/renderer/NVRHI/RTXPT, .gitmodules, and
    neo/CMakeLists.txt.

Documentation map
-----------------

Read these first in a new session:

    docs/pathtracing_handoff.txt
    docs/pathtracing.txt
    docs/pathtrace_modules.txt
    docs/pathtrace_geometry_universe.md
    docs/pathtrace_material_universe.md
    docs/pathtrace_interactive_surfaces.md

Use them this way:

- pathtracing_handoff.txt: current operational handoff and next-step guidance.
- pathtracing.txt: broader chronology, current feature state, testing commands,
  known traps, and historical context.
- pathtrace_modules.txt: file-by-file PathTrace* module map and feature flow
  guide, including "what to touch / what to avoid" guidance.
- pathtrace_geometry_universe.md: guardrails for persistent geometry lookup
  cost, validation cost, lifecycle, thread ownership, material links,
  allocation, and transient geometry exclusions.
- pathtrace_material_universe.md: guardrails for persistent material facts,
  frame-local descriptor ownership, reset boundaries, key stability, validation
  cost, thread ownership, allocation, and churn stats.
- pathtrace_interactive_surfaces.md: Doom material/register/runtime-state guide
  for switchable fluorescent lights, colored panels, texture transforms, and
  Q2RTX off-camera emissive design implications.

Current feature state to preserve
---------------------------------

The current RT smoke path supports:

- Primary camera rays through NVRHI ray tracing.
- Static and dynamic BLAS/TLAS with separate geometry buffers.
- Persistent static-world geometry cache now owned by PathTraceGeometryUniverse,
  with static surface records and universe-owned key lookup/append completion.
- Per-frame dynamic geometry capture.
- RT-only CPU skinning for skinned MD5 characters as a temporary correctness
  bridge.
- Bindless material texture table using Texture.Load.
- Manual bilinear Texture.Load filtering.
- Doom diffuse YCoCg decode.
- Alpha any-hit for cutout/perforated materials.
- Albedo, normal, specular, and emissive texture discovery/diagnostics.
- Mode 14 selected point-light direct lighting with RT shadow rays.
- Mode 14 normal-map lighting using Doom/RBDoom's DXT5-style normal decode:
  normal X from texture alpha, normal Y from green, normal Z reconstructed.
- Mode 14 simple Blinn-style diagnostic specular from Doom spec maps.
- Mode 14 unshadowed emissive/glow preview from safe ambient/additive stages.
- Mode 18 toy one-bounce path tracing with selected Doom lights, texture
  sampling, force-ambient control, simple secondary rays, temporal accumulation,
  and lighting scale CVars.
- Mode 19 emissive triangle inventory/debug visualization. The CPU inventory is
  a broad material/triangle candidate list for future ReSTIR/PT work; the debug
  view samples emissive texture luminance and skips skinned/deformed character
  dissolve overlays.
- GUI/SWF texture and surface diagnostics behind opt-in CVars.
- Smoke particle placeholder dithering behind CVars.
- Portal/window and object-glass stochastic transparency placeholders.
- Debug-only selected-light sprite proxies for strobe/flare-like light shaders.
- Narrow callback-entity skip for custom-shader item shells to avoid the disk
  pickup/delete crash without hiding normal skinned/dynamic characters.
- Persistent material universe records with stable material indexes, material
  facts, emissive facts, and universe-derived active material table enabled by
  default.
- Per-draw-surface evaluated emissive state. Stable material facts describe
  whether a material can emit; capture records whether this specific draw
  surface is emitting right now from shader registers. Modes 18, 19, and 20
  honor that runtime state, including switchable/dimming fluorescent lights.
- Cached universe-derived active material table enabled by default through
  r_pathTracingMaterialCache.

Debug modes:

    0  hit/miss
    1  depth
    2  interpolated normal
    3  surface class
    4  UV
    5  geometric normal
    6  material ID hash color
    7  material table debug color
    8  sampled diffuse texture probe
    9  alpha/coverage preview
    10 albedo
    11 translucent overlay inspection
    12 translucent subtype classification
    13 fixed Lambert lighting
    14 selected point-light shadows/direct lighting
    15 selected point-light influence debug
    16 normal map preview
    17 specular map preview
    18 toy one-bounce path trace
    19 emissive triangle inventory/debug view
    20 single-frame reservoir direct emissive lighting

Current module/refactor state
-----------------------------

PathTracePrimaryPass used to own nearly everything. It is now intentionally
small: feature detection, per-frame entry, output sizing, and present/blit glue.
It still owns most persistent m_smoke* handles/counters. Static-world geometry
cache storage and static surface identity/append bookkeeping now live in
PathTraceGeometryUniverse.

Main module split:

- PathTracePrimaryPass.*: frame-level pass shell and persistent state owner.
- PathTraceSmokeSceneBuild.cpp: high-level per-frame scene build sequence.
- PathTraceSmokeResources.*: NVRHI smoke resource creation, reset, binding, and
  resource commit packaging.
- PathTraceSmokeDispatch.*: constants, resource transitions, dispatch,
  accumulation, and readback queueing.
- PathTraceSmokeReadback.cpp: optional debug readback logging.
- PathTraceAcceleration.*: BLAS/TLAS helpers, upload batch, static BLAS
  signature, and acceleration submit helpers.
- PathTraceSceneCapture.*: Doom draw-surface traversal, validation, bucketing,
  stats, and dynamic geometry append. Static surface cache identity and append
  completion now go through PathTraceGeometryUniverse.
- PathTraceGeometry.*: shared RT smoke vertex layout and pure geometry helpers.
- PathTraceGeometryUniverse.*: persistent static geometry universe scaffold,
  static surface records, static cache vectors, static lookup/append ownership,
  generation/stats, and future home for previous-frame geometry history.
- PathTraceSkinning.*: temporary CPU skinning bridge.
- PathTraceSurfaceClassification.*: surface bucket and translucent subtype
  classification.
- PathTraceDoomMaterialClassifier.*: Doom material/stage classification.
- PathTraceMaterialTextureDiscovery.*: visible material texture discovery and
  metadata registration.
- PathTraceTextureRegistry.*: safe texture metadata registry.
- PathTraceMaterialUniverse.*: persistent per-material records/facts and stable
  material universe indexes.
- PathTraceDynamicMaterialState.*: GPU-facing material table and cache.
- PathTraceGuiSurfaces.*: GUI surface admission gate.
- PathTraceEmissiveCandidates.*: emissive triangle candidate records and dumps.
- PathTraceLightSelection.*: selected debug light ranking.
- PathTraceDebugDumps.* and PathTraceSurfaceDebugDumps.*: terminal diagnostics
  and one-shot CVar trigger/reset behavior.
- PathTraceCVars.*: RT/PT CVar declarations and definitions.

The broad extraction phase is probably complete. Do not keep splitting purely
for line count. A future PathTraceSmokeState / PathTraceSmokeScene ownership
object may be useful, but that should be a deliberate architecture change.

Recent refactor/docs commits
----------------------------

Recent important commits on this branch:

    d40fe9fa Move static geometry lookup into universe
    ddc2ec19 Update path tracing docs for geometry universe
    e55ba1ab Add path trace geometry universe scaffold
    9a78a170 Cache universe material table work
    90719330 Default to material universe table
    d8e6ca22 Add universe-derived material table path
    7004b522 Add stable material universe indexes
    68237ab6 Upload emissive light candidate buffer
    5c94ac5c Add emissive light candidate summaries
    d37f1238 Add material universe facts
    4b179101 Use material universe for emissive inventory
    f79870ee Extract RT smoke material universe module
    4d1e5cda Add RT smoke material universe validation
    9338bcda Update path tracing refactor overview
    a4dca266 Add path trace module comments
    34be0103 Document path trace module map
    59f00215 Extract path trace scene resource commit packaging
    7b9a4f4c Move path trace smoke scene build
    355ac29e Extract path trace scene build diagnostic logs
    d2d06b57 Extract path trace diagnostic triggers
    c5194175 Extract path trace scene summary diagnostics
    c66ce58b Extract path trace slow scene diagnostics
    010724b6 Extract path trace material metadata registration
    2ba361d3 Extract path trace CVars
    c4165bdd Extract path trace scene buffer creation
    9f4df79e Extract path trace acceleration buffer uploads
    20db5d49 Extract path trace acceleration submit helpers
    6c8868d0 Extract path trace scene resource handoff
    406f71f5 Move path trace smoke resource lifecycle
    26a903f0 Move path trace smoke readback
    48b89221 Extract path trace smoke dispatch
    f0b593f4 Extract material texture discovery
    d70b9b30 Extract path trace light selection
    a61b595c Move path trace diagnostic dumps
    6909be3a Move path trace scene capture body
    bd24bacb Extract path trace surface diagnostics
    72621425 Extract path trace GUI surface diagnostics
    09fe3c38 Move material texture diagnostics
    ab82fcf8 Extract path trace acceleration helpers
    4a8d64c3 Extract path trace smoke binding resources
    5dae6458 Extract path trace dynamic material state
    be021f80 Add RT smoke scene timing details
    48ee632a Extract path trace scene capture helpers
    918569f4 Move emissive inventory dump
    e9b463c2 Extract path trace emissive candidate builder
    f1b853c0 Extract Doom material classifier helpers
    4b7aad17 Move path trace material texture registry state
    9eb1475a Extract path trace texture safety helpers
    1896089e Extract path trace debug name helpers
    4b35f553 Extract path trace GUI surface helper
    72b2645e Extract path trace skinning helpers
    cd4d0716 Extract path trace geometry helpers

Build and copy workflow
-----------------------

Preferred Windows Release/PT builds now use the CMake presets in
neo/CMakePresets.json. They keep performance testing on explicit Release build
lanes and avoid accidentally launching the legacy Debug executable.

Configure/build PT retail:

    cmake --preset win64-pt-retail
    cmake --build --preset win64-pt-retail-release

Configure/build PT dev:

    cmake --preset win64-pt-dev
    cmake --build --preset win64-pt-dev-release

Configure/build PT dev with Optick:

    cmake --preset win64-pt-dev-optick
    cmake --build --preset win64-pt-dev-optick-release

Equivalent .bat wrappers in neo:

    cmake-vs2022-win64-pt-retail.bat
    cmake-vs2022-win64-pt-dev.bat
    cmake-vs2022-win64-pt-dev-optick.bat
    cmake-build-vs2022-win64-pt-retail-release.bat
    cmake-build-vs2022-win64-pt-dev-release.bat
    cmake-build-vs2022-win64-pt-dev-optick-release.bat

Copy the preset-built executable from the matching build folder, for example:

    Copy-Item -LiteralPath E:\prog\rbdoom-3-bfg-rt\build-win64-pt-dev\Release\RBDoom3BFG.exe -Destination E:\prog\rbdoom-3-BFG-prebuilt\RBDoom3BFG.exe -Force

Do not use the legacy Debug executable path for PT validation:

    cmake --build build --config Debug --target RBDoom3BFG

This path is kept only as a compile/link sanity check. It is broken for branch
validation: the generated legacy project can be stale until `cmake -S neo -B
build` is rerun after NVRHI source additions, and the resulting
`build\Debug\RBDoom3BFG.exe` is not representative for RT/PT behavior or
performance. Do not copy it into the prepared game folder for normal PT testing.

Avoid this old Debug copy path except when deliberately testing the broken
legacy lane:

    Copy-Item -LiteralPath E:\prog\rbdoom-3-bfg-rt\build\Debug\RBDoom3BFG.exe -Destination E:\prog\rbdoom-3-BFG-prebuilt\RBDoom3BFG.exe -Force

If pathtrace_smoke.rt.hlsl changes, also build/copy the RT shader blob:

    cmake --build build --config Debug --target PathTracingShaders

    Copy-Item -LiteralPath E:\prog\rbdoom-3-bfg-rt\base\renderprogs2\spirv\builtin\pathtracing\pathtrace_smoke.rt.bin -Destination E:\prog\rbdoom-3-BFG-prebuilt\base\renderprogs2\spirv\builtin\pathtracing\pathtrace_smoke.rt.bin -Force

If a new .cpp is added under neo/renderer/NVRHI, rerun:

    cmake -S neo -B build

The legacy Debug project does not reliably pick up new globbed NVRHI
PathTrace sources until this regeneration is done. This is another reason not
to treat `cmake --build build --config Debug --target RBDoom3BFG` as the branch
validation path.

For preset builds, rerun the relevant configure preset instead:

    cmake --preset win64-pt-dev

Testing baseline
----------------

The user has repeatedly validated the refactor in game. Recent tested checks:

- Game launches.
- Mode 18 ray tracing still works.
- Mode 19 emissive inventory/debug still works.
- Crosshair material dump still prints material details.
- GUI surfaces still appear when enabled.
- Diagnostics still trigger and reset.
- Scene capture, skinning, texture/material diagnostics, and emissive polygon
  classifier stayed active across the extraction series.
- Material universe active table path works without observed material jumping
  after row-order compatibility fixes.
- Geometry universe scaffold and static lookup/append move were tested by the
  user with no visual regressions or crashes. User observed possible modest
  early/high-FPS uplift, with the old longer-session decay still present but no
  worse.

Useful quick runtime tests:

    r_pathTracing 1
    r_pathTracingDebugMode 18
    r_pathTracingDebugMode 19
    r_pathTracingCrosshairMaterialDump 1
    r_pathTracingGuiDump 1
    r_pathTracingClassDump 1
    r_pathTracingSmokeLog 1

For CPU timing:

    r_pathTracingTimingLog 1
    r_pathTracingTimingThreshold 0
    r_pathTracingSmokeLog 0
    r_pathTracingReadbackEnable 0

For material universe active-table validation:

    r_pathTracingMaterialUniverseTable 1
    r_pathTracingMaterialUniverseTableValidate 1
    r_pathTracingTimingLog 1
    r_pathTracingTimingThreshold 0
    r_pathTracingSmokeLog 0

Clean validation reports materialTablePath=universe and
universeTableCompare=1/0 in timing logs. A mismatch while the universe table is
active falls back to the legacy table for that frame and logs
materialTablePath=legacyFallback.

Remember idTech persists CVars between sessions. Do not assume defaults.

Material universe status
------------------------

The material side is now fairly complete for easy single-threaded wins:

- PathTraceMaterialUniverse owns persistent material records/facts.
- Persistent records store stable material facts only. Per-frame descriptor
  indexes, visible texture bindings, texture probe state, and compact active
  material table row order stay in PathTraceDynamicMaterialState.
- Stable material universe indexes exist and are used by emissive summaries and
  uploaded light candidate records.
- Persistent lookup is keyed by materialId plus a 64-bit material-name hash.
  The shader-facing materialId remains the Doom material-name hash used by
  triangle material arrays and active material tables, but the stronger
  persistent key reduces collision/oscillation risk.
- BeginSmokeMaterialUniverseFrame resets frame-local churn counters before the
  material table path. GetSmokeMaterialUniverseStats reports cumulative and
  frame-local hits, misses, rebuilds, signature checks, validation checks, and
  validation mismatches.
- ReserveSmokeMaterialUniverse reserves the persistent unordered_map before
  material table builds. Future level-load estimates should reserve closer to
  expected level material count.
- ClearSmokeMaterialUniverse exists for full reset boundaries: level unload,
  renderer shutdown, vid_restart, map changes, or an explicit future PT cache
  reset command.
- PathTraceDynamicMaterialState builds the active shader-facing material table
  from universe records by default.
- r_pathTracingMaterialCache caches the universe-derived active table by default.
- Static triangle material-index compatibility is preserved by keeping the
  compact active row order compatible with cached static material-index buffers.
- Optional validation can compare universe-derived and legacy active tables.
- Persistent-record validation remains gated by
  r_pathTracingMaterialUniverseValidate and defaults to off in normal gameplay.
- Mutation is render/main-thread owned. Worker jobs may prepare immutable
  material candidates, but should not mutate the persistent material universe
  map directly without a deliberate synchronization design.

Do not casually change active material row order. It is tied to static cached
triangle material-index buffers. If changing this, use
r_pathTracingMaterialUniverseTableValidate and test mode 18 in material-heavy
areas.

Geometry universe status
------------------------

Current geometry universe implementation:

- PathTraceGeometryUniverse owns the persistent static-world cache vectors:
  surface keys, vertices, indexes, triangle classes, and triangle material IDs.
- It owns static surface records:
  RtSmokePersistentStaticSurfaceRecord with key, class/material IDs,
  current and previous vertex/index/triangle ranges, per-frame lifecycle flags,
  history validity, dirty flag, and geometry format enum.
- It owns static surface lookup:
  HasStaticSurface / FindStaticSurface through a key-to-record lookup map.
- It owns append policy entry/completion:
  CanAppendStaticSurface, BeginStaticSurfaceAppend, CompleteStaticSurfaceAppend.
- It owns per-frame update bookkeeping:
  BeginFrame, TouchStaticSurface, and EndFrame mark static records as seen,
  newly created, disappeared, history-valid, invalidated, or dirty.
- Static record validation is reported through existing smoke diagnostics:
  validate=total/range/duplicate-key/history/key-vector when
  r_pathTracingGeometryUniverseValidate is enabled. Clean tested frames report
  validate=0/0/0/0/0.
- PathTraceSceneCapture still builds the actual static vertex/index/class/material
  data, but it touches cached static surfaces through the universe and completes
  new appends through the universe.
- PathTraceSmokeSceneBuild still consumes the same shader-facing arrays, so the
  shader/resource contract is unchanged.
- No previous-frame GPU buffers or motion-vector output exists yet.

The next geometry work should be deliberate and behavior-preserving. The current
records are still a bridge: they encode identity, current/previous ranges, and
lifecycle state, but they do not yet own full previous-position buffers or
dynamic/rigid/skinned history.

Important guardrails:

- Keep TouchStaticSurface on the key-to-record lookup path; do not return it to
  per-surface O(n) scans.
- Keep expensive validation and duplicate-key checks behind
  r_pathTracingGeometryUniverseValidate or another explicit diagnostic gate.
- Persistent records are for static/semi-static geometry with stable identity,
  not muzzle flashes, particles, sparks, or one-frame transient effects.
- The universe is single-owner mutable render-thread state unless a deliberate
  synchronization design is added.
- Record materialId is a persistent Doom/material-universe link, not a direct
  shader table row.

Critical DLSS Ray Reconstruction / motion-vector requirement
------------------------------------------------------------

The user discovered this requirement during geometry-universe planning:

- Stock Q2RTX does not support DLSS Ray Reconstruction because it cannot produce
  the required motion-vector output.
- A Q2RTX Ray Reconstruction fork exists at:

      E:\prog\references\Q2RTX-rayreconstruction-master

- That fork is useful as an architecture reference. In particular, its shader
  path stores/uses previous triangle positions and previous instance transforms,
  then projects current and previous hit positions to produce motion vectors.

Design rule for this branch:

    PathTraceGeometryUniverse must be designed to retain one previous frame of
    geometry/history data. Geometry-derived motion vectors are a core future
    requirement, not a denoiser/RR afterthought.

Why this matters:

- Camera-only motion vectors are insufficient for moving rigid entities,
  animated/skinned meshes, reflected/refracted surfaces, and disocclusions.
- Frame-local geometry rebuilds lose the identity/history needed for temporal
  reconstruction.
- If the universe only stores "current packed PathTraceSmokeVertex arrays",
  adding DLSS RR later could require rebuilding the geometry ownership model.

The long-term target:

- Each persistent surface/triangle record should be able to map current and
  previous geometry.
- For static world, previous positions can often be identical or share the same
  range.
- For rigid entities, store current and previous transforms or equivalent
  current/previous world-space ranges.
- For skinned/deformed meshes, eventually store previous skinned positions or
  provide a GPU skinning history source.
- Records need invalidation/disocclusion flags when previous-frame mapping is
  missing or untrustworthy.
- Future GPU buffers should allow current/previous position data, position
  deltas, or another explicit geometry-history representation.

Compression/quantization note:

- Static mesh compression or quantization may be useful later for bandwidth/VRAM
  and cache behavior.
- Do not implement it before stable records/history and buffer ownership exist.
- Leave room in records for a geometry format enum/descriptor and bounds or
  quantization basis.
- Avoid making "std::vector<PathTraceSmokeVertex> forever" the permanent
  universe contract.

Geometry universe next-step plan
--------------------------------

Completed current step:

- PathTraceGeometryUniverse now has BeginFrame / TouchStaticSurface / EndFrame.
- Static records track lastSeenFrame, previousSeenFrame, seenThisFrame,
  newlyCreatedThisFrame, disappearedThisFrame, previousRangeValid, historyValid,
  and dirty.
- Static records carry explicit previous vertex/index/triangle offsets and
  counts. For static world, consecutive-frame history promotes the current
  range as the previous range because the cached static geometry is unchanged.
- Existing smoke logs report seen/new/gone, history/previous-range, dirty, and
  validation counters. User-tested mode 18 showed no crashes or visible
  regressions, history=N/N on stable seen surfaces, dirty=0 when standing still,
  and validate=0/0/0/0/0.

Recommended next step:

1. Normalize current/previous range fields into explicit small range structs.

   This is a code-cleanliness step before changing buffers:

   - current vertex/index/triangle range,
   - previous vertex/index/triangle range,
   - helper validation for both ranges,
   - no shader/resource behavior changes.

2. Add a one-shot geometry-universe diagnostic dump if validation fails.

   Useful dump fields:

   - record key,
   - current and previous ranges,
   - seen/new/gone/history/dirty flags,
   - validation failure category.

3. Later, add previous-frame/range history beyond static identity.

   Start with static world:

   - Static world records can mark previous range valid when unchanged.
   - Current and previous ranges may be identical for static surfaces.
   - This is the safest way to validate the history model before dynamic objects.

   Then rigid entities:

   - They need stable identity separate from static world keys.
   - Store current/previous model transforms and range mapping.
   - Do not rely on Doom draw-surface order as identity.

   Then skinned/deformed:

   - The current CPU skinning path is temporary.
   - Long term probably wants GPU-side skinned data or a history source.
   - Do not build a large CPU threading solution for this unless profiling proves
     it is worth it.

4. Later, move static GPU buffer ownership/dirty uploads into the universe.

   Only after CPU records/history are stable:

   - persistent static vertex/index/class/material buffers,
   - dirty ranges,
   - current/previous position or delta buffers,
   - buffer format descriptor/enum,
   - BLAS build/update policy tied to dirty state.

5. Later, generate motion vectors.

   This requires shader/resource contract work:

   - output motion/depth buffers,
   - current and previous view/projection matrices,
   - current/previous hit position reconstruction,
   - sky/background motion,
   - invalid/disoccluded motion handling,
   - eventually reflection/refraction motion if those paths are added.

   Use the Q2RTX Ray Reconstruction fork as a design reference, not as code to
   drop in wholesale.

Critical Q2RTX / ReSTIR light-list lesson
-----------------------------------------

The Q2RTX Ray Reconstruction reference fork was checked specifically for
off-camera emissive behavior. Its useful lesson is not "keep the last visible
emissive surface alive." Q2RTX promotes emissive surfaces into an explicit light
sampling contract:

- Static emissive BSP surfaces become persistent light polygon records.
- Those light records are assigned to BSP clusters and gathered into
  per-cluster light lists using PVS plus a light-influence test.
- ReSTIR samples the light list for the cluster containing the shaded point,
  not the set of emissive surfaces visible to the current camera.
- Dynamic/model emissive lights are transformed and injected into the same
  cluster light lists each frame.
- Temporal ReSTIR history uses previous-to-current light identity remapping. If
  a previous dynamic light cannot be matched to a current light, that history is
  invalidated.

Direction adjustment for this branch:

- The current reservoir scaffold is still valid and should be kept.
- Do not treat final ReSTIR input as "visible emissive candidates plus a little
  persistence." That will keep producing screen-space-like behavior.
- Static world emissives should become persistent light-universe records that
  can be sampled independently of current camera visibility.
- Stable rigid/entity emissives, such as object-like vending-machine panels,
  need persistent identity plus current transform and some future
  area/portal/cluster-style membership.
- Game-logic emissives, such as switchable or dimming fluorescent lights, need
  persistent identity but per-frame evaluated active/color/intensity state from
  drawSurf->shaderRegisters.
- Truly transient or currently unmatchable dynamic emissives should remain
  current-frame injections until previous-to-current identity and matching
  occluder geometry are trustworthy.
- Temporal reuse should wait for stable light identity, previous-to-current
  remapping, runtime emissive state signatures, and geometry/occluder
  confidence.

The next useful ReSTIR work is therefore light-universe quality and spatial
membership diagnostics before spatial or temporal reuse. A Doom-side equivalent
does not have to clone Q2RTX clusters exactly, but it needs some area/portal or
leaf-style membership so off-camera emissives can affect nearby shaded points
without relying on current screen visibility.

Immediate next-work suggestions
-------------------------------

The heavy broad-refactor phase is complete. Next work should be focused and
specific.

1. CPU-bound performance investigation

   The user observed that raising mode 18 output resolution to 4K did not
   materially change FPS, strongly suggesting a CPU-side bottleneck.

   First use existing timing logs to identify whether time is in:

   - scene capture
   - CPU skinning
   - material metadata discovery
   - material table build
   - buffer creation/upload
   - BLAS/TLAS submit
   - dispatch preparation

   Low-risk CPU optimizations to consider:

   - Reuse/reserve per-frame scratch vectors.
   - Avoid repeated static-world capture/material work.
   - Strengthen material metadata caching.
   - Add more timing/counters around PathTraceSceneCapture and CPU skinning.

   Avoid building a large CPU job system unless profiling identifies a large,
   isolated, data-parallel hotspot. GPU-side skinning and GPU-side light prep are
   likely better long-term answers than elaborate CPU multithreading.

2. Optick profiling

   Optick is integrated but behind CMake option OPTICK, default OFF. Existing
   evidence:

   - neo/cmake-vs2022-win64-optick-profiling.bat exists.
   - USE_OPTICK controls whether OPTICK_EVENT macros compile in.
   - win_main.cpp has OPTICK_FRAME("MainThread").
   - engine/backend/GPU markers exist.
   - PT scene build/dispatch and cinematic decode scopes have been added in the
     hot paths that were useful during the GUI/video investigation.
   - No obvious gameplay CVar for starting/stopping captures was found.

   Useful if doing a small prep pass:

   - Build with -DOPTICK=ON.
   - Try external Optick capture again.
   - Keep new scopes boring and non-behavioral. Avoid broad console breadcrumb
     spam unless actively chasing a non-returning hitch; the temporary
     r_pathTracingPhaseLog/r_pathTracingPhaseSlowThreshold path was removed.

   If capture remains awkward, the current r_pathTracingTimingLog path is still
   simpler for immediate CPU bottleneck work.

3. ReSTIR/PT bare-minimum vertical slice

   Do not start by trying to integrate all of NVIDIA RTXPT or by reviving the
   removed Donut/RTXPT checkout. Current status:

   - Mode 19 emissive inventory now has a real GPU light-candidate contract:
     area, normal, UV bounds, material universe index, emissive texture slot,
     placeholder weight/PDF fields, and stable-ish identity.
   - PathTraceReservoirs.* now owns current/previous/spatial reservoir buffers.
     Reservoir history is cleared when output/camera/FOV, material table,
     emissive candidate records, light-candidate records, or static-BLAS
     signature changes. Use r_pathTracingReservoirDump 1 to inspect the state.
   - Mode 20 now does single-frame reservoir direct lighting from emissive
     candidates and writes current reservoir records. It is intentionally noisy
     and has no spatial/temporal reuse yet.
   - PathTraceLightUniverse.* now keeps static emissive candidates persistent by
     stable-ish identity, can promote stable rigid/object-like emissives into
     semi-static dynamic records, and merges persistent static records with
     current-frame dynamic emissives. Missing dynamic records are tracked but
     not injected by default because their matching occluder geometry is not
     persistent yet. Skinned/particle/non-rigid dynamic emissives remain
     frame-local.
     Use r_pathTracingLightUniverseDump 1 to inspect static seen/new/missing,
     semi-static dynamic promoted/missing/aged counts, dynamic frame count, and
     merged upload count.
   - Relevant mode 20/light-universe CVars:
     r_pathTracingReservoirTwoSidedEmissives,
     r_pathTracingLightUniversePersistDynamic,
     r_pathTracingLightUniverseInjectMissingDynamic,
     r_pathTracingLightUniverseDynamicMinSeenFrames,
     r_pathTracingLightUniverseDynamicMaxMissingFrames.
   - Important material/runtime split: PathTraceMaterialUniverse stores stable
     emissive capability/facts only. Per-frame capture owns evaluated runtime
     emissive state from drawSurf->shaderRegisters, including game-logic driven
     on/off/dimming behavior. Do not regress this back to material-only
     emissive decisions.
   - Mode 20 is a useful scaffold/debug view, not the final ReSTIR input model.
     Q2RTX points toward a proper light universe with spatial membership:
     persistent static lights, current dynamic injections, explicit
     previous-to-current light id remapping, and invalidation when a dynamic
     light cannot be matched.
   - Add candidate-origin diagnostics before reuse: visible/frame candidate,
     persistent static light, current dynamic injection, promoted/semi-static
     rigid light, and runtime/game-driven light state.
   - Start planning Doom-side spatial membership for light candidates before
     spatial or temporal reuse. This may be area/portal/leaf-style rather than
     a direct Q2RTX cluster clone.
   - Add spatial reuse only after the light list/candidate origin model is less
     screen-visible dependent.
   - Add temporal reuse/reprojection only after stable light identity,
     previous-to-current remapping, runtime emissive state signatures, and
     matching geometry/occluder confidence are available.

   Files likely involved:

   - PathTraceEmissiveCandidates.*
   - PathTraceDynamicMaterialState.*
   - PathTraceSmokeResources.*
   - PathTraceSmokeDispatch.cpp
   - pathtrace_smoke.rt.hlsl
   - PathTraceCVars.*

   Needed data contracts:

   - GPU material record with base color, alpha/cutout, normal/specular/roughness
     approximation, emissive color/texture, and unsupported/fallback flags.
   - Emissive candidate buffer with area, normal, material/emissive texture slot,
     approximate radiance/luminance, weight/pdf fields, and stable-ish identity.
   - Reservoir buffers exist for current/previous/intermediate data; shading
     does not consume them yet.
   - Reset rules currently cover camera, FOV, output size, static BLAS,
     material table, emissive candidates, and light candidates.

4. Doom 3 interactive GUI surfaces

   Current direct-GUI-surface tracing is diagnostic only. Long-term direction:

   - Keep Doom's GUI/SWF raster path for panel contents.
   - Render each live panel offscreen or expose its composited texture.
   - Project that composited texture onto the physical panel surface.
   - Keep generated painter-order GUI quads out of the TLAS.

   Start in PathTraceGuiSurfaces.*, PathTraceSceneCapture.*, and
   PathTraceMaterialTextureDiscovery.* if working here. Preserve diagnostics
   before broadening behavior.

   Important: do not reintroduce the old GuiModel.cpp generated-frontEndGeo or
   layer-offset experiment as a normal path. It made Doom's painter-order GUI
   layers become physical geometry, broke raster monitor/video screens, and
   produced repeatable GUI/cinematic hitches. GuiModel.cpp has been restored to
   original behavior: generated GUI drawSurfs have frontEndGeo = NULL. The RT
   path should eventually consume a composited panel texture on the physical
   screen surface, not the generated UI quads themselves.

5. Build/profile configuration notes

   - FFMPEG=ON/BINKDEC=OFF and FFMPEG=OFF/BINKDEC=ON were both tested in Debug
     for the monitor/video route. Decoder choice did not change the remaining
     behavior after GuiModel.cpp was restored.
   - RETAIL=ON only defines ID_RETAIL. It strips/gates shipping-vs-dev behavior
     and is not an optimization switch for Visual Studio multi-config builds.
     It was an early suspect because the first fresh clean RT build was
     retail-like, but a fresh generic Release non-retail control also ran at the
     120 FPS cap. Do not treat ID_RETAIL as the proven cause of the stall.
   - PATH_TRACING_DEV defines ID_PATH_TRACING_DEV on top of ID_RETAIL. Add
     future PT-only diagnostics or commands under this define for the explicit
     PT Release build lanes.
   - OPTICK=ON is available through win64-pt-dev-optick and the matching batch
     files. Keep it off for final performance sanity checks.
   - The legacy Visual Studio Debug compile path is broken for PT validation.
     It can be used as a compile/link sanity check only, and even then the
     generated project may need `cmake -S neo -B build` after NVRHI source
     additions. Do not copy `build\Debug\RBDoom3BFG.exe` into the prepared game
     folder for normal PT testing. Use the preset Release/PT lanes instead.
     Debug executables are expected to be about 30 MB here while Release
     executables are about 14-15 MB. The difference comes from /Od, /Zi, /RTC1,
     debug CRT, and incremental debug linking. The legacy Debug executable
     reproduced the broken raster/PT frame-pacing performance, while fresh
     Release builds were clean.
   - r_useValidationLayers defaults to 1, which enables NVRHI validation.
     Launching Debug with `+set r_useValidationLayers 0` restored raster to the
     120 FPS cap, so validation was the main raster-side Debug multiplier. Use
     validation only for targeted resource/state/binding investigations.
   - PT remains much slower in Debug/RelWithDebInfo even with validation off.
     The PT prototype does substantial CPU staging every frame: Doom draw-surface
     capture, vertex transforms, material metadata/table work, emissive
     inventory, buffer upload setup, and dynamic BLAS/TLAS preparation. Debug
     CRT allocation checks, disabled/reduced inlining, /Od, /RTC1, and
     debug-friendly codegen make that path non-representative. RelWithDebInfo is
     optimized but this generated project still uses MultiThreadedDebugDLL and
     weaker inlining than Release.
   - Clean upstream/master was bisected from v1.6.0 to babd04b3 with matching
     generated renderprogs2 and tested clear. Do not chase upstream again unless
     a clean upstream Release build starts reproducing.
   - r_pathTracing must be enabled before device creation for RT feature support
     to be available. If PT appears dead in a Release build, launch with
     +set r_pathTracing 1 or ensure the config sets it before renderer startup.

Design traps to avoid
---------------------

- Do not call idMaterial::GetEditorImage() during RT texture discovery.
- Do not force-load missing images during frame capture.
- Do not switch default RT texture sampling back to SampleLevel until the
  SWF/GUI device-removal issue is understood.
- Do not reuse a cached BLAS with mismatched shader-side attribute buffers.
- Do not treat Doom draw-surface order as stable identity.
- Do not casually merge static and dynamic triangle metadata. InstanceID matters.
- Do not broaden callback-entity capture without testing the disk pickup/delete
  crash scenario.
- Do not treat dynamic/game-logic-driven materials as static material truth.
  Improve diagnostics and narrow bridges first.
- Do not let generated GUI draw commands become persistent TLAS geometry. They
  are UI painter-order layers, not physical panels.
- Do not revive the removed in-tree RTXPT/Donut/CMake leftovers unless doing a
  deliberate clean integration.

End-of-phase recommendation
---------------------------

This is a good point to end high-reasoning broad refactor work and return to
focused, measured tasks:

- one CPU performance hotspot at a time,
- one Doom material family at a time,
- one GUI-surface experiment at a time,
- one ReSTIR/PT vertical slice at a time.

The module split is now readable enough that future sessions should start from
docs/pathtrace_modules.txt, inspect only the relevant files, and avoid reopening
the entire refactor unless a real ownership problem appears.
